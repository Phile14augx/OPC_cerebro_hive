name: Preview Deployments

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  deployments: write
  pull-requests: write

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        
      - name: Placeholder for Preview Deployment
        run: echo "This workflow will deploy PR previews to Vercel/Cloudflare Pages once an integration is added."
