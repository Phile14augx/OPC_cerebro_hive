describe('Transformation Integration', () => {
  it('TODO: should apply transformations and publish to evaluation lab', () => {
    // Stub
  });

  it('TODO: should fail and report telemetry if anonymization is down', () => {
    // Stub
  });
});
