# EXP-0003: GraphRAG vs Naive RAG — Comprehensiveness and Indexing Cost Validation

```yaml
id: "EXP-0003"
title: "GraphRAG vs Naive RAG — Comprehensiveness, Diversity, and Indexing Cost on Enterprise Corpus"
source_knowledge:
  - "KN-KG-000001"      # GraphRAG — evidence B; arXiv 2404.16130; MIT; 35K+ stars
  - "KN-INFRA-000001"   # MCP — knowledge graph core is the integration target
  - "KN-AGENT-000004"   # Agent Memory — Tier 3 (semantic memory) may use GraphRAG

hypothesis: >
  Running GraphRAG (Microsoft, arXiv 2404.16130) over a representative Cerebro enterprise
  knowledge corpus will produce query responses that are measurably more comprehensive and
  diverse than naive vector-similarity RAG, validating the paper's claim of "substantial
  improvement over conventional RAG for global sensemaking queries." GraphRAG indexing cost
  will be quantified to assess production viability given the paper's "expensive operation"
  warning. Acceptable: ≤$500 indexing cost for a 10,000-document corpus at production scale.

success_criteria:
  - "GraphRAG global queries: comprehensiveness score ≥20% better than naive RAG (matching paper claims)"
  - "GraphRAG global queries: diversity score ≥25% better than naive RAG (matching paper claims)"
  - "GraphRAG local queries: performance comparable to naive RAG (within ±10%)"
  - "Indexing cost: ≤$500 USD for 10,000-document representative corpus"
  - "Indexing time: ≤4 hours for 10,000-document corpus on reasonable compute"
  - "Query latency: GraphRAG global query ≤30s p95 (acceptable for knowledge search; async use case)"

failure_criteria:
  - "Comprehensiveness improvement <10% — not worth indexing cost"
  - "Indexing cost >$2,000 for 10,000 docs — economically unviable at scale"
  - "Indexing time >24 hours for 10,000 docs — operationally unviable"
  - "GraphRAG local queries >50% worse than naive RAG — unacceptable regression for point lookups"
  - "Graph construction fails on enterprise corpus structure (non-text artifacts, code, structured data)"

evaluation_dataset: >
  Representative enterprise knowledge corpus (synthetic/anonymized only — NO real tenant data):
  
  2,000 documents across 5 categories (to keep indexing cost measurable):
  - 400 technical design documents (architecture docs, ADRs, RFCs)
  - 400 code review discussions and PR comments
  - 400 support tickets and incident reports
  - 400 meeting notes and decision logs
  - 400 research summaries and competitive analysis docs
  
  Query set: 50 queries split by type:
  - 25 "global" queries (cross-document synthesis): 
      "What are the recurring themes in our incident reports?"
      "Which architectural decisions have the most downstream dependencies?"
      "What is the team's consensus on microservices vs monolith?"
  - 25 "local" queries (point lookups):
      "What did the ADR say about selecting pgvector?"
      "Find the incident report for the October 2025 outage."
      "Who approved the k8s migration decision?"
  
  Evaluation metric (matching arXiv 2404.16130 methodology):
  - LLM-as-judge on 5-point scale for: comprehensiveness, diversity, empowerment
  - Human spot-check on 10% of query/response pairs
  - Baseline: naive pgvector cosine similarity search (top-5 chunks)

scope: >
  Two conditions:
  A. Naive RAG: pgvector cosine similarity, top-5 chunk retrieval, GPT-4o synthesis
  B. GraphRAG: Microsoft graphrag Python library (pip install graphrag), same GPT-4o synthesis
  
  Indexing both conditions on 2,000-document corpus.
  50 queries run against both conditions.
  Cost tracking: all LLM API calls logged with token counts and prices.
  
  NOT in scope: GraphRAG integration into packages/knowledge-graph-core (that follows if successful).
  NOT in scope: real tenant data — synthetic/anonymized corpus only.
  NOT in scope: hybrid retrieval (GraphRAG local + naive RAG combined) — defer to follow-up.

components_involved:
  - "packages/knowledge-graph-core"   # target integration if experiment succeeds
  - "services/llm-gateway"            # all LLM calls for indexing and synthesis routed here
  - "services/evaluation-service"     # LLM-as-judge evaluation framework

dependencies:
  - "Microsoft graphrag Python package installed (pip install graphrag>=0.3)"
  - "2,000-document synthetic corpus generated and reviewed"
  - "pgvector baseline (naive RAG) pipeline set up using existing packages/knowledge-graph-core"
  - "LLM-as-judge prompt calibrated against 5 human-evaluated reference pairs"
  - "Cost tracking: governance-api cost logging enabled for experiment namespace"

time_box: "2 weeks"

engineering_effort: MEDIUM
# Estimated: 1 engineer × 2 weeks.
# Week 1: corpus generation + naive RAG baseline + GraphRAG indexing
# Week 2: 50-query evaluation + LLM-as-judge scoring + cost analysis + recommendation

risk_level: MEDIUM
# Primary risk: indexing cost overrun. GraphRAG requires LLM calls for entity extraction +
# community summarization. For 2,000 docs, estimated $100–800 depending on doc length.
# Mitigation: start with 200-doc pilot in Day 1; extrapolate cost before running full 2,000.

expected_cost: "<$500 USD"
# Estimation for 2,000-doc corpus:
# GraphRAG indexing (entity extraction): 2,000 docs × 1,500 tokens avg × $10/1M (GPT-4o) = $30
# GraphRAG indexing (community summaries): 500 communities × 2,000 tokens × $10/1M = $10
# GraphRAG queries (50 global): 50 × 5,000 tokens (MapReduce) × $10/1M = $2.50
# Naive RAG queries (50): 50 × 2,000 tokens × $10/1M = $1
# LLM-as-judge (100 eval pairs × 2 conditions): 100 × 1,500 tokens × $10/1M = $1.50
# Buffer: 3× for retries and prompt tuning = ~$135 total
# Budget cap: $500 USD (abort experiment if cost tracking shows >$200 at 25% completion)

expected_outputs:
  - "Comprehensiveness score comparison: GraphRAG vs naive RAG per query type (global vs local)"
  - "Diversity score comparison: GraphRAG vs naive RAG"
  - "Cost breakdown: indexing cost per document; query cost per global vs local query"
  - "Latency benchmark: GraphRAG global query p50/p95 vs naive RAG p50/p95"
  - "Graph quality assessment: entity count, community count, community coherence sample"
  - "Go/no-go recommendation for packages/knowledge-graph-core GraphRAG integration"
  - "Optimal corpus size estimate: at what doc count does GraphRAG become cost-effective?"

next_steps_if_successful:
  - "Integrate GraphRAG indexing pipeline into packages/knowledge-graph-core"
  - "Add GraphRAG as query mode to knowledge graph API (alongside existing vector search)"
  - "Route 'global sensemaking' queries (cross-doc synthesis) to GraphRAG; route 'local' queries to pgvector"
  - "Create tenant-level GraphRAG index management (index per workspace or per knowledge base)"
  - "Promote GraphRAG in TECHNOLOGY-RADAR from TRIAL to ADOPT if production validation passes"
  - "Create EXP-0004: Hybrid retrieval (GraphRAG global + naive local) vs pure GraphRAG"

next_steps_if_failed:
  - "If cost >$2,000/10K docs: evaluate GraphRAG-Light (edge-only, no community summaries)"
  - "If comprehensiveness improvement <10%: re-evaluate with better chunking strategy (overlap + metadata)"
  - "If indexing time unacceptable: evaluate DRIFT search (GraphRAG local variant, faster)"
  - "If graph construction fails on code/structured data: limit GraphRAG to prose-only corpus sections"
  - "Document findings as cost/quality caveat in KN-KG-000001"

status: PROPOSED
owner: "TBD — assign to knowledge-graph-core + evaluation-service team"
proposed_start: "2026-Q3 (after EXP-0002 reasoning routing validation completes)"

pilot_plan: >
  Day 1 BEFORE running full experiment:
  1. Select 200-document pilot subset (40 per category)
  2. Run GraphRAG indexing on pilot corpus
  3. Record: tokens used, cost, time elapsed
  4. Extrapolate to 2,000 docs: if projected cost >$300, abort and report findings
  5. Extrapolate to 10,000 docs: document cost estimate for production planning
  This pilot-first approach bounds financial risk to <$50 on Day 1.
```
