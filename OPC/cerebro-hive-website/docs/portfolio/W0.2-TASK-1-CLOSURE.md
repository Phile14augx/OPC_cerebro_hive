# W0.2 Task 1 Closure — Authoritative Workspace Classification

## Status

Task 1 is complete. The audit engine enumerates the declared pnpm workspace graph, records the monorepo root separately as control-plane scope, classifies every required contract, preserves classified defects as findings, and fails closed without modifying any workspace contract or CI workflow.

VCT remains 0. W0.3 has not started.

## Structural Reconciliation

| Measure | Result |
| --- | ---: |
| Declared workspaces | 141 |
| Root control-plane entries | 1 |
| Source-bearing workspaces | 132 |
| Source-absent workspaces | 9 |
| Contract records | 705 |
| Filesystem findings in this checkout | 0 |
| Required baseline fixtures | 11/11 green |
| Extended Task 1 fixtures | 14/14 green |
| Unclassified contracts | 0 |
| Unknown classifications | 0 |
| Duplicate workspace paths | 0 |
| Stale manifest paths | 0 |
| Workspace graph mismatches | 0 |
| Root-scope violations | 0 |

The dangling Windows junction remains a regression fixture. `packages/eda-knowledge/internal` does not exist in this checkout and is not an active repository finding.

## Classification Totals

| Classification | Count |
| --- | ---: |
| `REAL` | 165 |
| `ABSENT-BY-DESIGN` | 0 |
| `BROKEN` | 332 |
| `PLACEHOLDER` | 0 |
| `FALSE-GREEN` | 35 |
| `NOT-APPLICABLE` | 173 |
| **Total** | **705** |

No `ABSENT-BY-DESIGN` classification was invented. That state requires a rationale, owner, and review evidence, none of which can be inferred from package metadata.

## Findings by Contract Type

| Contract | `REAL` | `BROKEN` | `FALSE-GREEN` | `NOT-APPLICABLE` | Defect findings |
| --- | ---: | ---: | ---: | ---: | ---: |
| build | 46 | 87 | 0 | 8 | 87 |
| test | 13 | 87 | 33 | 8 | 120 |
| typecheck | 75 | 55 | 2 | 9 | 57 |
| lint | 31 | 102 | 0 | 8 | 102 |
| schema/config | 0 | 1 | 0 | 140 | 1 |
| **Total** | **165** | **332** | **35** | **173** | **367** |

The schema/config finding belongs to `packages/db`, where `prisma/schema.prisma` exists without a declared schema validation contract.

## Semantic Candidate Reconciliation

The initial scan resolves to 36 candidate scripts across 34 packages:

- 33 canonical `test` contracts use `--passWithNoTests` and are classified `FALSE-GREEN`.
- `apps/platform-api` and `services/forge-api` declare `typecheck: "exit 0"` and are classified `FALSE-GREEN`.
- `services/forge-api` also has a non-canonical `test:break` script using `--passWithNoTests`; it is retained as script-level semantic evidence under the test contract.

The category rollup contains 35 `FALSE-GREEN` contract records because the canonical test and `test:break` evidence for `services/forge-api` belong to the same workspace contract category.

## Artifacts

- `artifacts/w0.2/workspace-validation-inventory.json` — authoritative graph and source/contract evidence.
- `config/workspace-validation-classification.json` — complete 141-workspace classification manifest.
- `artifacts/w0.2/workspace-validation-findings.json` — complete non-green findings set grouped by contract.

## Scope Boundary

Task 1 did not repair `forge-api`, `identity-core`, Studio, Twin Studio, any other workspace contract, or any GitHub Actions workflow. It did not modify runtime/product architecture or begin W0.3.

The next movement is Task 2: semantic triage of the preserved findings and creation of the reviewed exception manifest. Hotspot repairs remain deferred until Task 2 stabilizes.
