# ADR 0004: Design Token Pipeline
**Status**: Accepted
**Decision**: All styling relies on a single source of truth (Figma → Style Dictionary → JSON → Tailwind → CSS Variables → TypeScript). Hardcoded values are forbidden.
