{
  "name": "@cerebro/memory-service",
  "version": "0.1.0",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run"
  },
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "@cerebro/memory-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
