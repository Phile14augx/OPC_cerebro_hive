{
  "name": "@cerebro/agent-sdk",
  "version": "0.1.0",
  "description": "CerebroHive Agent SDK providing the framework-agnostic agent abstractions.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint --max-warnings=0 .",
    "dev": "tsc -w",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cerebro/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
