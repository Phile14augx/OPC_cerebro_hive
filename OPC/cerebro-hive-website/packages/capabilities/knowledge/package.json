{
  "name": "@cerebro/knowledge-capability",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@cerebro/domain": "workspace:*",
    "@cerebro/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^22.0.0"
  }
}
