{
  "name": "@cerebro/workflow-capability",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@cerebro/db": "workspace:*",
    "@cerebro/domain": "workspace:*",
    "@temporalio/client": "^1.20.3",
    "@temporalio/workflow": "^1.20.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
