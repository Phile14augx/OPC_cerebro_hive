{
  "name": "@cerebro/observability-api",
  "version": "0.1.0",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint \"src/**/*.ts\"",
    "build": "tsc"
  },
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "@tanstack/react-query": "^5.0.0",
    "@cerebro/events": "workspace:*"
  }
}
