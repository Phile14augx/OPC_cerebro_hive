---
phase: 01-schema-navigation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/studio/app/(platform)/app/navigation/index.ts
  - apps/studio/app/(platform)/app/navigation/lookup.ts
  - apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx
  - apps/studio/app/(platform)/app/[...segments]/page.tsx
  - scripts/audit-nav-routes.mjs
  - vitest.studio.config.ts
  - tests/unit/placeholder-module.test.tsx
  - .planning/phases/01-schema-navigation-foundation/01-NAV-STATUS.md
autonomous: true
requirements: [NAV-01, NAV-02]
must_haves:
  truths:
    - "D-01: A user who navigates to any of the 99 registry destinations lands on a page, never a Next.js 404"
    - "D-05: A destination in one of the 8 previously-unscoped placeholder groups renders through this plan's shared PlaceholderModule/lookup/catch-all machinery"
    - "D-09: Route identifiers in the registry stay stable — findNavEntryByPath resolves by href, so later phases can flip a planned item to real without renaming the tree"
    - "D-14: A user who follows a stale or unregistered link (not just a registry item) still lands on an honest placeholder, not a 404"
    - "A destination with no backing feature shows the module name, the feature name, and its status"
    - "A user who follows a stale or unregistered /app/* link still lands on an honest placeholder, not a 404"
  artifacts:
    - path: "apps/studio/app/(platform)/app/navigation/index.ts"
      provides: "implementationStatus on every one of the 99 NavItems"
      contains: "implementationStatus"
    - path: "apps/studio/app/(platform)/app/navigation/lookup.ts"
      provides: "Single shared registry path-to-entry resolver reused by catch-all and breadcrumbs (D-08)"
      exports: ["findNavEntryByPath", "findNavTrailByPath", "allNavGroups"]
    - path: "apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx"
      provides: "The single canonical 'not yet available' component (D-06/D-07)"
      exports: ["PlaceholderModule"]
    - path: "apps/studio/app/(platform)/app/[...segments]/page.tsx"
      provides: "Registry-driven catch-all route covering every /app/* path without a literal page.tsx"
    - path: "scripts/audit-nav-routes.mjs"
      provides: "Automated NAV-01 gate — static audit of registry hrefs against the App Router tree"
  key_links:
    - from: "apps/studio/app/(platform)/app/[...segments]/page.tsx"
      to: "apps/studio/app/(platform)/app/navigation/lookup.ts"
      via: "findNavEntryByPath(path)"
      pattern: "findNavEntryByPath"
    - from: "apps/studio/app/(platform)/app/[...segments]/page.tsx"
      to: "apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx"
      via: "component render"
      pattern: "PlaceholderModule"
    - from: "apps/studio/app/(platform)/app/navigation/lookup.ts"
      to: "apps/studio/app/(platform)/app/navigation/index.ts"
      via: "platformNavigation import"
      pattern: "platformNavigation"
---

<objective>
Deliver the end-to-end "honest destination" slice: a user can navigate to any `/app/*` path in the navigation registry and land on either the existing real page or a single, standardized, registry-driven "not yet available" page — never a 404, never fabricated data.

Purpose: this is the foundation slice every other Phase 1 plan builds on. Plans 01-04, 01-05 and 01-06 all consume `PlaceholderModule` and the lookup module created here.
Output: `implementationStatus` on all 99 registry items, a shared lookup module, the `PlaceholderModule` component, the catch-all route, and the two Wave-0 verification harnesses (route audit script and component render test).
</objective>

<phase_goal>
**As a** CerebroHive Studio user, **I want to** click any destination in the sidebar and land on either a working page or a clearly-labelled "not yet available" page, **so that** I never hit a 404, a dead link, or fabricated data while navigating the product.

> Note: the ROADMAP `**Goal:**` line for Phase 1 is not written in user-story format. The story above is *derived* from the ROADMAP goal plus success criteria 1-3, not invented scope. Run `/gsd mvp-phase 1` to canonicalise it in ROADMAP.md.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-navigation-foundation/01-CONTEXT.md
@.planning/phases/01-schema-navigation-foundation/01-RESEARCH.md
@.planning/phases/01-schema-navigation-foundation/01-PATTERNS.md
@.planning/phases/01-schema-navigation-foundation/01-UI-SPEC.md
@.planning/phases/01-schema-navigation-foundation/01-VALIDATION.md

<corrections_to_research>
Two claims in 01-RESEARCH.md and 01-PATTERNS.md were re-verified against the working tree at plan time and are **wrong**. Trust this block over those documents:

1. **`platformNavigation` DOES include `forgeNavigation` and `hiveopsNavigation`.** `apps/studio/app/(platform)/app/navigation/index.ts` lines 247-262 export a 14-element array containing all 14 groups: `workspaceNavigation, forgeNavigation, hiveopsNavigation, aiNavigation, solutionsNavigation, infrastructureNavigation, dataNavigation, securityNavigation, automationNavigation, researchNavigation, academyNavigation, businessNavigation, talentNavigation, supportNavigation`. RESEARCH.md Pattern 1 and PATTERNS.md both claim `forgeNavigation` is excluded — it is not. Do NOT write `[...platformNavigation, forgeNavigation]`; that would duplicate all 19 forge items.
2. **`apps/studio/app/(platform)/app/talent/assessments/` exists as a directory but contains no `page.tsx`.** `/app/talent/assessments` is still a 404 today. Directory existence is not route existence — the audit script must key on `page.tsx` files, not directories.

Item count confirmed: 4+19+7+9+5+9+7+7+6+5+5+6+5+5 = **99 items across 14 groups**.
</corrections_to_research>

<interfaces>
Current `apps/studio/app/(platform)/app/navigation/index.ts` types (lines 25-36) — extend `NavItem` in place, do not create a parallel type:

  export type NavItem = { title: string; href: string; icon?: LucideIcon };
  export type NavGroup = { title: string; href?: string; icon: LucideIcon; items: NavItem[] };
  export const platformNavigation: NavGroup[]   // 14 groups, 99 items total

Existing design-system primitives to compose from (`apps/studio/app/(platform)/app/components/ui/`):

  Card    — forwardRef<HTMLDivElement, HTMLAttributes<HTMLDivElement>>, base classes
            "rounded-2xl border border-border bg-surface text-text-primary shadow-sm overflow-hidden"
  Badge   — cva-based; variants default | secondary | destructive; use variant="secondary"
  cn      — from "./utils"

Existing jsdom test precedent to mirror (`vitest.dashboard.config.ts`): `environment: "jsdom"`, `setupFiles: ["./test/setup.ts"]`, and no `@vitejs/plugin-react` needed (root `tsconfig.json` sets `"jsx": "react-jsx"`, esbuild handles the transform).
</interfaces>

<decisions_implemented>
This plan is the direct implementation of:
- **D-01:** all 99 registered navigation destinations resolve — zero 404s — via `implementationStatus` on every registry item plus the catch-all route.
- **D-05:** the foundation that lets the 8 previously-unscoped placeholder groups (HiveOps, Infrastructure, Data, Automation, Research, Academy, Business, Support = 50 items) render a standardized placeholder — this plan builds the shared `PlaceholderModule`/lookup/catch-all machinery those groups render through.
- **D-06/D-07/D-08:** `PlaceholderModule`'s registry-driven architecture and copy pattern (already cited in the artifacts table above).
- **D-09:** route identifiers stay stable — the lookup module (`findNavEntryByPath`) resolves by registry `href`, so later phases can flip a `planned` item to a real page with zero navigation-tree renaming.
- **D-14:** broken/unregistered hardcoded links (not just the 99 registry items) are covered too — the catch-all's no-match fallback renders `PlaceholderModule` instead of calling `notFound()`.
</decisions_implemented>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the two Wave-0 verification harnesses (both must FAIL on current code)</name>
  <files>scripts/audit-nav-routes.mjs, vitest.studio.config.ts, tests/unit/placeholder-module.test.tsx</files>
  <read_first>
    - scripts/audit/audit-routes.ts — existing filesystem route-enumeration precedent; reuse its recursive `getAppRoutes` approach for collapsing `(group)` folders and skipping `api/`
    - apps/studio/app/(platform)/app/navigation/index.ts — the registry the script parses (99 items, 14 groups)
    - apps/studio/app/(platform)/app/components/Sidebar.tsx — lines 23-33 (the hand-picked `sections` array) and lines 35-39 (the `pinnedFavorites` array) that the script must cross-check
    - vitest.dashboard.config.ts — the jsdom plus setupFiles config shape to mirror
    - test/setup.ts — the shared `@testing-library/jest-dom` matcher setup
    - .planning/phases/01-schema-navigation-foundation/01-VALIDATION.md — Wave 0 Requirements section
  </read_first>
  <action>
Create `scripts/audit-nav-routes.mjs` as a plain Node ESM script (no `tsx` — `tsx` is not a repo-root dependency; existing root scripts such as `scripts/repo-health.mjs` and `scripts/check-sitemap.mjs` are the `.mjs` plus `node` convention to follow). It performs a static audit with no dev server and no build:

  1. Read `apps/studio/app/(platform)/app/navigation/index.ts` as text. Extract every exported `NavGroup` const by matching `export const <name>Navigation: NavGroup = {` blocks, capturing the group's `title:` and `href:` values and each item's `title:`, `href:` and (once Task 2 lands) `implementationStatus:` value.
  2. Enumerate real routes by walking `apps/studio/app/(platform)/app` recursively, recording a route for every `page.tsx`. Collapse Next.js group folders (directory names wrapped in parentheses) into the parent path and skip any directory named `api`. Record separately whether a catch-all segment directory (a directory name starting with `[...`) exists directly under `apps/studio/app/(platform)/app`.
  3. Assertions, each producing a named failure line and a non-zero exit code:
     - `MISSING_STATUS`: every registry item has an `implementationStatus` value. Report the count as `n/99`.
     - `INVALID_STATUS`: every value is one of `active`, `planned`, `disabled`.
     - `UNRESOLVED_ROUTE`: every registry `href` — item hrefs plus every group-level `href` — is either backed by a literal `page.tsx` route or covered by the root catch-all. If no catch-all directory exists, every href without a literal route is UNRESOLVED.
     - `ACTIVE_WITHOUT_PAGE`: no item marked `active` lacks a literal `page.tsx`.
     - `PINNED_ORPHAN`: every `href` string inside `Sidebar.tsx`'s `pinnedFavorites` array appears as a registry `href`.
     - `SIDEBAR_HANDPICK`: `Sidebar.tsx`, with `//`-prefixed comment lines stripped before matching, contains zero occurrences of the hand-pick expression `platformNavigation.find(g => g.title ===`. This is the D-13 reachability gate consumed by plan 01-04.
  4. Print a summary line `audit-nav-routes: <n> registry items, <p> passing, <f> failing` and exit 0 only when every assertion passes.

Create `vitest.studio.config.ts` at the repo root, mirroring `vitest.dashboard.config.ts`: `environment: "jsdom"`, `include: ["tests/unit/**/*.test.tsx"]`, `setupFiles: ["./test/setup.ts"]`.

Create `tests/unit/placeholder-module.test.tsx` importing `PlaceholderModule` from `../../apps/studio/app/(platform)/app/components/ui/PlaceholderModule` and asserting, via `@testing-library/react`'s `render` plus `screen`, the locked copy strings from 01-UI-SPEC.md's Copywriting Contract:
  - rendering with `group="Automation" title="Workflow Builder" status="planned"` shows the exact eyebrow text `Automation / Workflow Builder`, the exact heading `Not yet available`, the exact body `This module is part of the CerebroHive platform but is not enabled in this release.`, and the exact badge text `Status: Planned`
  - rendering with `status="disabled"` shows `Status: Disabled`
  - the rendered markup contains no `text-foreground` and no `text-muted-foreground` class (shadcn tokens are forbidden in the platform tree per 01-UI-SPEC.md)

Both harnesses are expected to FAIL at the end of this task — the script because no item has `implementationStatus` and no catch-all exists, the test because `PlaceholderModule` does not exist yet. Do not stub anything to make them pass.
  </action>
  <verify>
    <automated>node scripts/audit-nav-routes.mjs || echo EXPECTED-FAIL-OK</automated>
    <automated>pnpm vitest run --config vitest.studio.config.ts --reporter=dot || echo EXPECTED-FAIL-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-nav-routes.mjs` exits non-zero and its output contains the literal token `MISSING_STATUS`
    - The script's output reports `99` as the total registry item count
    - `pnpm vitest run --config vitest.studio.config.ts --reporter=dot` exits non-zero with a module-resolution failure naming `PlaceholderModule`
    - `vitest.studio.config.ts` sets `environment: "jsdom"` and `setupFiles: ["./test/setup.ts"]`
    - `scripts/audit-nav-routes.mjs` contains no `import` of a `.ts` source file (it parses text; it does not execute TypeScript)
    - The script's `UNRESOLVED_ROUTE` check covers group-level `href` values as well as item hrefs (verify by reading the collection step)
  </acceptance_criteria>
  <done>Both Wave-0 gates exist, run, and fail for the correct reasons: no `implementationStatus` values and no `PlaceholderModule`.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the honest-destination slice — registry status, shared lookup, PlaceholderModule, catch-all route</name>
  <files>apps/studio/app/(platform)/app/navigation/index.ts, apps/studio/app/(platform)/app/navigation/lookup.ts, apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx, apps/studio/app/(platform)/app/[...segments]/page.tsx, .planning/phases/01-schema-navigation-foundation/01-NAV-STATUS.md</files>
  <read_first>
    - apps/studio/app/(platform)/app/navigation/index.ts — all 262 lines; the file being modified
    - apps/studio/app/(platform)/app/components/ui/Card.tsx — the Card primitive to compose from
    - apps/studio/app/(platform)/app/components/ui/Badge.tsx — the Badge primitive and its `secondary` variant
    - apps/studio/app/(platform)/app/components/ui/utils.ts — the `cn` helper
    - apps/studio/app/(platform)/app/evaluations/[view]/page.tsx — the `"use client"` plus `useParams` routing convention to follow, and (lines 44-50) the bare-string "Coming Soon" ANTI-PATTERN this component supersedes; do not replicate it
    - .planning/phases/01-schema-navigation-foundation/01-UI-SPEC.md — Component Contracts section 1 is the locked composition and copy for PlaceholderModule
    - .planning/phases/01-schema-navigation-foundation/01-PATTERNS.md — PlaceholderModule and catch-all reference implementations
  </read_first>
  <action>
**A. Extend the registry (`navigation/index.ts`), modify in place — per D-06/D-09.**
Add `implementationStatus: "active" | "planned" | "disabled"` to the `NavItem` type as a **required** field so TypeScript enforces coverage of all 99 items. Assign a value to every item using this rule, applied in order:

  1. `active` — the href has a literal `page.tsx` in the App Router tree AND that page renders data obtained from a real backend call (a `fetch`, a `useForge*` hook, `forgeApi`, or an `/api/` route). Confirm each candidate by reading the page before marking it `active`.
  2. `planned` — everything else, including pages whose `page.tsx` exists but renders hardcoded/fabricated arrays or `setTimeout`-driven fake actions. Fabricated content is exactly what D-15 and NAV-02 prohibit; an existing fake page is not `active`.

Concrete assignments already established by CONTEXT.md D-03/D-05 and verified at plan time — apply these exactly:
  - `forgeNavigation`: `active` for the 9 backed items — Forge Overview (`/app/forge`), AI Planner (`/app/forge/planner`), Requirements Studio (`/app/forge/requirements`), Architecture Studio (`/app/forge/architect`), Code Generation (`/app/forge/codegen`), Testing Intelligence (`/app/forge/testing`), AI Code Review (`/app/forge/review`), Deployment Studio (`/app/forge/deploy`), AI Documentation (`/app/forge/docs`). `planned` for the other 10 — UI/UX Studio, Backend Studio, Database Studio, API Studio, Mobile Studio, Web Studio, Desktop Studio, CerebroBots, Repository Manager, Monitoring & Ops.
  - `hiveopsNavigation`: all 7 items `planned`. D-05 names HiveOps as a placeholder group this phase, and all 7 pages were verified at plan time to render hardcoded arrays with zero backend calls.
  - `infrastructureNavigation` (9), `automationNavigation` (6), `researchNavigation` (5), `academyNavigation` (5), `businessNavigation` (6), `supportNavigation` (5): all `planned` — none has a `page.tsx`.
  - `dataNavigation` (7), `securityNavigation` (7), `workspaceNavigation` (4), `aiNavigation` (9), `solutionsNavigation` (5), `talentNavigation` (5): apply the rule above per item. Note `/app/talent/assessments` has a directory but **no** `page.tsx` — it is `planned`. Any item you mark `active` must be justified by an actual backend call found while reading its page.

Record the resulting per-item classification as a markdown table in `.planning/phases/01-schema-navigation-foundation/01-NAV-STATUS.md` (columns: group, item title, href, status, reason) so the demotion of existing-but-fake pages is auditable rather than silent.

**B. Create the shared lookup module (`navigation/lookup.ts`) — per D-08, one lookup, many consumers.**
Export `allNavGroups: NavGroup[]` sourced directly from `platformNavigation` (which already contains all 14 groups including `forgeNavigation` and `hiveopsNavigation` — see `<corrections_to_research>`; do NOT spread `forgeNavigation` in again).

Export `findNavEntryByPath(pathname: string): { group: NavGroup; item: NavItem } | null` resolving in two passes:
  1. Exact match on `item.href` across every group.
  2. Fallback: exact match on a group's own `href`. Two groups declare a `href` that no item shares — `securityNavigation.href` is `/app/security` (its items point at `/app/trust/security`, `/app/security/iam`, `/app/security/roles`, `/app/security/secrets`, `/app/trust/audit`, `/app/trust/compliance`, `/app/trust/policies`) and `supportNavigation.href` is `/app/support` (its items are all under `/app/support/`). Without this pass, Topbar's existing hardcoded links to `/app/security` and `/app/support` would fall through to the `Unknown` branch. On a group-href match, return a synthetic item `{ title: group.title, href: group.href, implementationStatus: "planned" }` so the eyebrow reads `Security / Security` rather than `Unknown / /app/security`.

Export `findNavTrailByPath(pathname: string): { label: string; href?: string }[]` returning `[{ label: group.title }, { label: item.title }]` for a matched path and `[]` otherwise — plan 01-04 consumes this for breadcrumbs and page titles.

This module must be import-safe from both server and client components: no `"use client"`, no React imports, no `next/navigation` imports.

**C. Create `components/ui/PlaceholderModule.tsx`.**
Implement exactly the locked composition from 01-UI-SPEC.md Component Contracts section 1. Props: `{ group: string; title: string; status: "planned" | "disabled" }`, with no `onClick` or CTA prop. Outer wrapper `flex items-center justify-center min-h-[60vh]`; a `Card` with `max-w-md p-8 text-center space-y-4`; an icon chip `w-12 h-12 mx-auto rounded-xl bg-surface-elevated border border-border` centering the `lucide-react` `Construction` icon at `size={22}` in `text-text-muted`; eyebrow `{group} / {title}` in `text-xs font-[var(--font-weight-heading)] uppercase tracking-widest text-text-muted`; heading `Not yet available` in `text-lg font-space font-[var(--font-weight-heading)] text-text-primary`; body `This module is part of the CerebroHive platform but is not enabled in this release.` in `text-sm text-text-secondary`; a `Badge variant="secondary" className="text-xs"` reading `Status: Planned` or `Status: Disabled`.

Use only the platform token set (`text-text-primary`, `text-text-secondary`, `text-text-muted`, `bg-surface`, `bg-surface-elevated`, `border-border`); never `text-foreground` or `text-muted-foreground`. Never apply `text-primary-accent` — this is a deliberately neutral informational state. Use the `font-[var(--font-weight-heading)]` token form, not a literal `font-bold`. Use exactly one icon choice across every placeholder render; do not vary the icon per group.

**D. Create the catch-all route `app/(platform)/app/[...segments]/page.tsx`.**
A `"use client"` component reading `useParams<{ segments: string[] }>()` from `next/navigation`, joining segments into `"/app/" + segments.join("/")`, calling `findNavEntryByPath`, and rendering `PlaceholderModule` with `group={group.title}`, `title={item.title}`, and `status` from `item.implementationStatus` (coerced to `"planned"` when the value is `active`, since an `active` item reaching the catch-all means its real page is missing). When no registry entry matches — a stale link such as Sidebar's `pinnedFavorites` entry, per Pitfall 3 — render `PlaceholderModule` with `group="Unknown"` and `title={path}`, matching the 01-UI-SPEC.md Copywriting Contract's `Unknown / {path}` format. Never call `notFound()`; D-01 mandates zero 404s phase-wide.

Verify Assumption A1 from 01-RESEARCH.md while doing this: after the build, confirm that `/app/ai/models` (nested under the existing `ai/` static folder, which has only a top-level `page.tsx`) resolves through the root catch-all. If it does not, add a per-prefix catch-all under each affected static folder and record the deviation in the plan SUMMARY.

Do NOT touch `apps/studio/lib/navigation/registry.ts` or `apps/studio/components/layout/StudioShell.tsx` — they are a second, unrelated registry that does not drive `/app/**` routing (RESEARCH.md Pitfall 1). Editing them is a silent no-op.
  </action>
  <verify>
    <automated>node scripts/audit-nav-routes.mjs</automated>
    <automated>pnpm vitest run --config vitest.studio.config.ts --reporter=dot</automated>
    <automated>pnpm --filter @cerebro/studio typecheck</automated>
    <automated>pnpm --filter @cerebro/studio build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-nav-routes.mjs` reports zero failures for `MISSING_STATUS`, `INVALID_STATUS`, `UNRESOLVED_ROUTE`, `ACTIVE_WITHOUT_PAGE` and `PINNED_ORPHAN`. Only `SIDEBAR_HANDPICK` may still fail — plan 01-04 owns it
    - The script reports `99/99` items carrying an `implementationStatus`
    - `pnpm vitest run --config vitest.studio.config.ts --reporter=dot` passes every `placeholder-module` assertion
    - Reading the `forgeNavigation` block shows exactly 9 items with `implementationStatus: "active"` and 10 with `"planned"`
    - Reading the `hiveopsNavigation` block shows all 7 items with `implementationStatus: "planned"`
    - With `//` comment lines stripped, `apps/studio/app/(platform)/app/[...segments]/page.tsx` contains zero occurrences of `notFound`
    - `apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx` contains zero occurrences of `text-foreground`, `text-muted-foreground`, `text-primary-accent`, and the literal class `font-bold`
    - `apps/studio/app/(platform)/app/navigation/lookup.ts` contains zero occurrences of `"use client"` and zero occurrences of the identifier `forgeNavigation` (it derives everything from `platformNavigation`)
    - `findNavEntryByPath("/app/security")` and `findNavEntryByPath("/app/support")` both return a non-null entry via the group-href fallback
    - `pnpm --filter @cerebro/studio build` completes with exit code 0
    - `.planning/phases/01-schema-navigation-foundation/01-NAV-STATUS.md` exists and contains one table row per registry item
  </acceptance_criteria>
  <done>Every registry destination resolves: literal pages where they exist, `PlaceholderModule` via the catch-all everywhere else, plus an honest fallback for unregistered `/app/*` paths. Both Wave-0 gates are green apart from the sidebar assertion owned by plan 01-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next.js App Router | User-controlled URL path segments reach the catch-all route's `useParams` |
| browser -> forge-api (`services/forge-api`, port 4005) | Not exercised by this plan, but the registry marks 9 forge routes `active`, making them reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `[...segments]/page.tsx` unregistered-path fallback | mitigate | Render only the requested path string inside `PlaceholderModule`'s eyebrow via React's default text escaping. Never use `dangerouslySetInnerHTML`, never echo query strings or hash fragments, never emit a stack trace or the internal route table |
| T-01-02 | Tampering | Catch-all shadowing a future real route | accept | Next.js resolves literal static routes before catch-all segments by design, so adding a real `page.tsx` in Phases 2-8 takes precedence with no change to the catch-all. Verified in Task 2 by confirming `/app/ai/models` falls through while `/app/ai` does not |
| T-01-03 | Spoofing / Elevation of Privilege | `services/forge-api` — 9 controllers have no `@UseGuards` and no enforced tenant/org scoping | accept (deliberately deferred) | Known pre-existing gap, explicitly out of Phase 1 scope per CONTEXT.md D-04 and 01-VALIDATION.md's Security Domain note. The shared tenant-scoping helper is WKSP-05 / Phase 2. Recorded so it is never mistaken for "already secure". This plan does not widen the gap — it only sets a registry status field |
| T-01-SC | Tampering | npm/pnpm installs | mitigate | No packages are installed by this plan. `vitest`, `jsdom`, `@testing-library/react` and `@testing-library/jest-dom` are already repo-root devDependencies. 01-RESEARCH.md's Package Legitimacy Audit records zero new packages this phase, with no `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts, so no blocking legitimacy checkpoint applies |
</threat_model>

<verification>
1. `node scripts/audit-nav-routes.mjs` — all assertions except `SIDEBAR_HANDPICK` report zero failures
2. `pnpm vitest run --config vitest.studio.config.ts --reporter=dot` — green
3. `pnpm --filter @cerebro/studio typecheck` — green (the required `implementationStatus` field forces compile-time coverage of all 99 items)
4. `pnpm --filter @cerebro/studio build` — green, and the build output lists an `/app/[...segments]` route
</verification>

<success_criteria>
- All 99 registry items carry a valid `implementationStatus`
- `PlaceholderModule` renders the four locked copy strings verbatim and uses only platform design tokens
- No `/app/*` path — registered or not — can produce a Next.js 404
- `findNavEntryByPath` and `findNavTrailByPath` are the single lookup, ready for plan 01-04's breadcrumbs, and they resolve group-level hrefs
- `01-NAV-STATUS.md` makes every `active` / `planned` classification auditable
</success_criteria>

<output>
Create `.planning/phases/01-schema-navigation-foundation/01-01-SUMMARY.md` when done.
Record in the SUMMARY: the Assumption A1 catch-all precedence result, the count of existing-but-fabricated pages demoted to `planned`, and any registry item whose classification was a judgment call.
</output>
