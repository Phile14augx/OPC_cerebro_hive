name: Enterprise Audit Governance

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

defaults:
  run:
    working-directory: OPC/cerebro-hive-website

jobs:
  audit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - uses: pnpm/action-setup@v4
      with:
        version: 9
    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '20'
        cache: pnpm
        cache-dependency-path: OPC/cerebro-hive-website/pnpm-lock.yaml

    - name: Install dependencies
      working-directory: ./OPC/cerebro-hive-website
      run: pnpm install --frozen-lockfile

    - name: Run Enterprise Navigation & UI Audit
      working-directory: ./OPC/cerebro-hive-website
      run: pnpm --filter @cerebro/studio run audit:enterprise

    - name: Validate Enterprise Score
      working-directory: ./OPC/cerebro-hive-website
      run: |
        SCORE=$(node -e "const fs = require('fs'); const s = JSON.parse(fs.readFileSync('apps/studio/audit/scores.json')); console.log(s.accessibility)")
        if [ "$SCORE" -lt 90 ]; then
          echo "Accessibility score ($SCORE%) is below the required 90% threshold."
          exit 1
        fi
        
        # In a real environment, you might fail the build if there are broken links.
        # We can extract the broken links count from route-coverage-matrix.csv if needed.
        exit 0

    - name: Upload Audit Dashboard
      uses: actions/upload-artifact@v7
      with:
        name: enterprise-executive-dashboard
        path: OPC/cerebro-hive-website/apps/studio/audit/executive-dashboard.md
