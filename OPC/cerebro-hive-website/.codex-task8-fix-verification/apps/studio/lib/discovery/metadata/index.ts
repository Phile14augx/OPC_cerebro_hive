export { DEFAULT_METADATA } from './defaults';
export {
  buildPageMetadata,
  buildServiceMetadata,
  buildIndustryMetadata,
  buildProductMetadata,
  buildAcademyMetadata,
  buildResearchMetadata,
} from './builders';
