/**
 * Shared public entry point for CerebroArchive contracts.
 *
 * Contract types and schemas will be exported here as the archive services
 * begin to consume them. Keeping the module explicit lets TypeScript emit the
 * package entry point declared in package.json.
 */
export {};
