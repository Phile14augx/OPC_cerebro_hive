apiVersion: v1
kind: Service
metadata:
  name: platform-svc
  namespace: cerebro-hive
  labels:
    app.kubernetes.io/name: platform-svc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: platform-svc
  ports:
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP
