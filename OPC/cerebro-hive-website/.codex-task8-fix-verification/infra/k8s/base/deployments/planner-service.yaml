apiVersion: apps/v1
kind: Deployment
metadata:
  name: planner-service
  labels:
    app.kubernetes.io/name: planner-service
    app.kubernetes.io/part-of: cerebro-hive
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: planner-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: planner-service
        app.kubernetes.io/part-of: cerebro-hive
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8920"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: cerebro-hive
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: planner-service
          image: ghcr.io/cerebrohive/cerebro-hive/planner-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8920
              protocol: TCP
          envFrom:
            - secretRef:
                name: cerebro-hive-secrets
                optional: true
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "4Gi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: planner-service
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: planner-service
  labels:
    app.kubernetes.io/name: planner-service
    app.kubernetes.io/part-of: cerebro-hive
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: planner-service
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: planner-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: planner-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: planner-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: planner-service
