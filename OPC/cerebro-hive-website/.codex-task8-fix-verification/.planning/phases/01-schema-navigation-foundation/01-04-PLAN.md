---
phase: 01-schema-navigation-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/studio/app/(platform)/app/components/Sidebar.tsx
  - apps/studio/app/(platform)/app/components/Topbar.tsx
  - apps/studio/app/(platform)/app/components/ui/Breadcrumbs.tsx
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "A user can see and expand all 14 navigation groups in the sidebar, including HiveOps, Automation, Research, Academy, Business and Support"
    - "Every link a user can click in the sidebar leads to a destination that exists in the navigation registry"
    - "A user always knows which module and feature they are looking at, from a breadcrumb and page title derived from the same registry entry"
  artifacts:
    - path: "apps/studio/app/(platform)/app/components/Sidebar.tsx"
      provides: "Generic render of all 14 platformNavigation groups instead of 6 hand-picked ones"
      contains: "platformNavigation"
    - path: "apps/studio/app/(platform)/app/components/ui/Breadcrumbs.tsx"
      provides: "Studio-local breadcrumb component, no JsonLd, platform tokens only"
      exports: ["Breadcrumbs"]
    - path: "apps/studio/app/(platform)/app/components/Topbar.tsx"
      provides: "Breadcrumb strip and registry-derived page title"
      contains: "Breadcrumbs"
  key_links:
    - from: "apps/studio/app/(platform)/app/components/Sidebar.tsx"
      to: "apps/studio/app/(platform)/app/navigation/index.ts"
      via: "platformNavigation.map over all 14 groups"
      pattern: "platformNavigation"
    - from: "apps/studio/app/(platform)/app/components/Topbar.tsx"
      to: "apps/studio/app/(platform)/app/navigation/lookup.ts"
      via: "findNavTrailByPath(pathname)"
      pattern: "findNavTrailByPath"
---

<objective>
Make every navigation destination actually reachable and orienting: render all 14 registry groups in the sidebar (6 are currently invisible), fix the sidebar's stale hardcoded links, and add a breadcrumb strip plus page title sourced from the same registry entry that drives routing.

Purpose: NAV-01 says "every sidebar navigation destination resolves". Plan 01-01 made the routes resolve; a destination that never appears in the sidebar is still not resolved *from the sidebar*. CONTEXT.md D-13 puts this restructure explicitly in scope, D-14 puts stale hardcoded links in scope, and D-08 puts registry-derived breadcrumbs and titles in scope.
Output: a sidebar rendering 99 destinations across 14 groups, zero orphaned hrefs, and one lookup feeding both breadcrumbs and titles.
</objective>

<phase_goal>
**As a** CerebroHive Studio user, **I want to** click any destination in the sidebar and land on either a working page or a clearly-labelled "not yet available" page, **so that** I never hit a 404, a dead link, or fabricated data while navigating the product.

This plan owns the "click any destination in the sidebar" half — plan 01-01 made the destinations resolve, this one makes them reachable.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-navigation-foundation/01-CONTEXT.md
@.planning/phases/01-schema-navigation-foundation/01-RESEARCH.md
@.planning/phases/01-schema-navigation-foundation/01-PATTERNS.md
@.planning/phases/01-schema-navigation-foundation/01-UI-SPEC.md
@.planning/phases/01-schema-navigation-foundation/01-01-SUMMARY.md

<interfaces>
Consumed from plan 01-01 (`apps/studio/app/(platform)/app/navigation/lookup.ts`):

  allNavGroups: NavGroup[]
  findNavEntryByPath(pathname: string): { group: NavGroup; item: NavItem } | null
  findNavTrailByPath(pathname: string): { label: string; href?: string }[]

`NavItem` now carries a required `implementationStatus: "active" | "planned" | "disabled"`.

Registry facts verified at plan time (`apps/studio/app/(platform)/app/navigation/index.ts` lines 247-262): `platformNavigation` is a **14-element** array already containing `forgeNavigation` and `hiveopsNavigation`. 01-RESEARCH.md and 01-PATTERNS.md both claim `forgeNavigation` is excluded — that is wrong. Iterating `platformNavigation` covers all 99 items with no extra spread.

Sidebar's current structure (`Sidebar.tsx`, 266 lines):
  lines 23-33   `sections` array — hand-picks 6 groups by title string; the defect to remove
  lines 35-39   `pinnedFavorites` array — contains one stale href
  lines 96-129  workspace switcher block — leave untouched
  lines 131-162 pinned-favorites render block — leave the render untouched, fix the data
  lines 164-216 CerebroForge amber collapsible block — leave untouched, keep as its own special case
  lines 219-261 the generic section/item render JSX — reuse verbatim, do not restyle
</interfaces>

<scope_guard>
Per 01-UI-SPEC.md Component Contract §3 this is a **behavioural** fix, not a restyle:
- Reuse the existing section/item JSX exactly. Do NOT retrofit its literal `font-bold` classes to the `font-[var(--font-weight-heading)]` token form — that retrofit is explicitly out of scope for the existing block (it applies only to new code such as `PlaceholderModule` and `Breadcrumbs`).
- Newly-surfaced groups must be visually indistinguishable from the ones already rendering. No "new" badge, no highlight colour, no different spacing.
- Do NOT port `apps/studio/components/discovery/Breadcrumbs.tsx` — it emits `JsonLd` structured data for search indexing and uses shadcn `text-foreground` tokens. Studio is an internal authenticated app; reuse only its `{ label, href? }[]` prop shape.
- Do NOT touch `apps/studio/lib/navigation/registry.ts` or `apps/studio/components/layout/StudioShell.tsx` — a second, unused registry (RESEARCH.md Pitfall 1).
</scope_guard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render all 14 navigation groups and repair the sidebar's stale links (D-13, D-14)</name>
  <files>apps/studio/app/(platform)/app/components/Sidebar.tsx</files>
  <read_first>
    - apps/studio/app/(platform)/app/components/Sidebar.tsx — all 266 lines; the file being modified
    - apps/studio/app/(platform)/app/navigation/index.ts — the 14-group `platformNavigation` array (lines 247-262) and each group's `title`
    - .planning/phases/01-schema-navigation-foundation/01-UI-SPEC.md — Component Contract §3, the locked "no restyle" boundary
    - .planning/phases/01-schema-navigation-foundation/01-RESEARCH.md — Pitfall 2 (the hidden-groups defect) and Pitfall 3 (the stale pinned link)
    - scripts/audit-nav-routes.mjs — the `SIDEBAR_HANDPICK` and `PINNED_ORPHAN` assertions this task must turn green
  </read_first>
  <action>
**A. Replace the hand-picked `sections` array with a generic loop.**
Delete the `sections` const at lines 23-33 entirely. Derive the rendered sections from `platformNavigation` with a `.filter(...).map(...)` chain that excludes only `CerebroForge` (whose 19 items are already rendered by the dedicated amber collapsible block at lines 164-216 — rendering them twice is the failure mode to avoid). Every other group, including the six currently invisible ones (HiveOps, Automation, Research, Academy, Business, Support), flows through the same loop.

Three group headers currently carry ad-hoc display labels that the generic loop would otherwise change. Preserve them with a single small display-label override map rather than reintroducing per-group hand-picking: map registry title `AI` to displayed `AI Platform`, and registry title `Solutions` to displayed `Explore`. Every other group displays its registry `title` verbatim. The current `Data & Security` merge is cosmetic only — render `Data` and `Security` as two separate sections under their registry titles; do not preserve the merge, because merging requires a hand-pick.

The resulting code must contain **zero** occurrences of the expression `platformNavigation.find(g => g.title ===`. That literal is what the `SIDEBAR_HANDPICK` audit assertion checks for, and it is the pattern that let six groups disappear.

**B. Reuse the existing item-render JSX verbatim.** Keep the section wrapper (`px-3 py-3 border-t border-border/50`), the `h4` header (`text-xs font-bold uppercase tracking-widest text-text-muted px-2 mb-2`), the `space-y-0.5` item list, the `TrackedLink` with its `analyticsEvent="sidebar_nav_click"` / `analyticsCategory="sidebar"` / `analyticsLabel={item.title}` props, the `isActive` computation (`pathname === item.href || pathname.startsWith(item.href + '/')`), and both branches of the active/inactive class ternary exactly as written today. Do not restyle, do not change class strings, do not swap `font-bold` for a token.

**C. Fix the stale pinned-favourite link (D-14, Pitfall 3).** `pinnedFavorites` currently contains `{ title: "Workflows", href: "/app/automation/workflows", ... }`. No registry item has that href — the nearest real destination is `Workflow Builder` at `/app/automation/builder` (`automationNavigation`, line 131). Change the href to `/app/automation/builder`. Also change that entry's `title` to `Workflow Builder` so the pinned label matches the destination it opens. Its icon lookup currently grabs `automationNavigation.items[0]?.icon` (the Overview icon); point it at the `Workflow Builder` item's own icon instead. Verify the other two pinned entries (`/app/ai/studio`, `/app/ai/knowledge`) are both present as registry hrefs and leave them alone if so.

Do not add `implementationStatus`-conditional styling to sidebar items in this task. D-06's status field drives *route rendering*; visually greying out planned items in the nav is not in any requirement and would need a UI-SPEC amendment.
  </action>
  <verify>
    <automated>node scripts/audit-nav-routes.mjs</automated>
    <automated>pnpm --filter @cerebro/studio typecheck</automated>
    <automated>pnpm --filter @cerebro/studio build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-nav-routes.mjs` exits 0 with every assertion green, including `SIDEBAR_HANDPICK` and `PINNED_ORPHAN`
    - With `//` comment lines stripped, `Sidebar.tsx` contains zero occurrences of `platformNavigation.find(g => g.title ===`
    - `Sidebar.tsx` contains a `platformNavigation` iteration expression (`.map(` applied to `platformNavigation` or a `.filter(...)` of it)
    - `Sidebar.tsx` contains zero occurrences of the string `/app/automation/workflows`
    - `Sidebar.tsx` contains the string `/app/automation/builder`
    - The class strings `text-xs font-bold uppercase tracking-widest text-text-muted px-2 mb-2` and `bg-surface-elevated text-text-primary font-bold shadow-sm border border-border` are still present, unchanged
    - The CerebroForge amber block (`sidebar_forge_nav_click`, `bg-amber-500/10`) is still present and still renders `forgeNavigation.items`
    - No forge item renders twice: the generic loop excludes the `CerebroForge` group
    - `pnpm --filter @cerebro/studio build` exits 0
  </acceptance_criteria>
  <done>All 14 groups and all 99 destinations are reachable from the sidebar, styled identically to the groups that already rendered, with no orphaned hrefs.</done>
</task>

<task type="auto">
  <name>Task 2: Add registry-derived breadcrumbs and page titles (D-08)</name>
  <files>apps/studio/app/(platform)/app/components/ui/Breadcrumbs.tsx, apps/studio/app/(platform)/app/components/Topbar.tsx</files>
  <read_first>
    - apps/studio/app/(platform)/app/components/Topbar.tsx — all 400 lines; the header layout (lines ~340-394, three flex sections: left mobile-toggle plus WorkspaceMenu, centre search/command palette, right assistant/notifications/user menu) and the hardcoded `Link` hrefs at lines ~136, ~228, ~281, ~288, ~295
    - apps/studio/app/(platform)/app/navigation/lookup.ts — `findNavTrailByPath`, created by plan 01-01
    - apps/studio/components/discovery/Breadcrumbs.tsx — prop-shape reference ONLY; do not copy its body (it emits `JsonLd` and uses shadcn tokens)
    - .planning/phases/01-schema-navigation-foundation/01-UI-SPEC.md — Component Contract §2, the locked breadcrumb contract
    - apps/studio/app/(platform)/app/components/ui/Card.tsx — the platform design-token set to stay inside
  </read_first>
  <action>
**A. Create `components/ui/Breadcrumbs.tsx`** implementing the locked contract from 01-UI-SPEC.md §2. Props: `{ items: { label: string; href?: string }[] }`. Render each entry's `label` in `text-text-secondary`, separated by a `lucide-react` `ChevronRight` at `size={12}` with `className="text-text-muted"`. The last entry renders in `text-text-primary font-[var(--font-weight-heading)]` with no link (it is the current page). Entries carrying an `href` render as a `next/link` `Link`; entries without one render as plain text. Return `null` when `items` is empty so pages with no registry match show nothing rather than an empty chrome strip. This is new code, so it uses the `font-[var(--font-weight-heading)]` token form — never a literal `font-bold` — and only platform tokens. Emit no `JsonLd`, no `<script type="application/ld+json">`, and no `BreadcrumbList` schema: this is an authenticated internal dashboard, not an indexed page.

**B. Wire it into `Topbar.tsx`.** Call `findNavTrailByPath(pathname)` (using the `usePathname()` value the file already has available, or adding the hook if absent) and render `<Breadcrumbs items={trail} />` in the header's left flex section, after the mobile toggle and `WorkspaceMenu`, using the `text-text-secondary` / `text-text-muted` tokens already used throughout this file (e.g. lines ~112, ~128). Hide the strip on small viewports with the same responsive utility class convention the file already uses for its other secondary chrome, so the mobile header does not overflow.

**C. Set the page title from the same lookup (D-08's "one lookup, two consumers").** In `Topbar.tsx`, add a `useEffect` keyed on `pathname` that sets `document.title` to `{item title} · CerebroHive Studio` when `findNavTrailByPath` returns a trail, falling back to `CerebroHive Studio` when it does not. Derive the title from the trail's last entry — do not add a second registry lookup, and do not hardcode a title map.

**D. Repair Topbar's hardcoded links (D-14).** This file uses raw `next/link` hrefs that bypass the registry entirely. Cross-check each against the registry and fix any that have no registry entry:
  - `/app/organizations` — a `workspaceNavigation` item; leave as-is
  - `/app/business/billing` — a `businessNavigation` item; leave as-is
  - `/app/security` — this is `securityNavigation`'s **group** `href`; verify plan 01-01's `findNavEntryByPath` resolves group-level hrefs. If it does not, retarget this link to the group's real first destination, `/app/trust/security` (Security Overview)
  - `/app/support` — this is `supportNavigation`'s **group** `href` and the group has **no** item with that href (its items are `/app/support/assistant`, `/app/support/help`, `/app/support/tickets`, `/app/support/community`, `/app/support/status`). Retarget this link to `/app/support/help` (Help Center)

Record in the SUMMARY every hardcoded Topbar href you changed and why.
  </action>
  <verify>
    <automated>node scripts/audit-nav-routes.mjs</automated>
    <automated>pnpm --filter @cerebro/studio typecheck</automated>
    <automated>pnpm --filter @cerebro/studio build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/studio/app/(platform)/app/components/ui/Breadcrumbs.tsx` exists and exports `Breadcrumbs`
    - It contains zero occurrences of `JsonLd`, `application/ld+json`, `BreadcrumbList`, `text-foreground`, `text-muted-foreground`, and the literal class `font-bold`
    - It contains `ChevronRight` and `font-[var(--font-weight-heading)]`
    - `Topbar.tsx` contains `findNavTrailByPath` and renders `<Breadcrumbs`
    - `Topbar.tsx` contains exactly one call site for `findNavTrailByPath`, whose result feeds both the breadcrumb render and the `document.title` effect (D-08's single lookup)
    - `Topbar.tsx` contains zero occurrences of the href string `/app/support` that is not part of a longer path such as `/app/support/help`
    - `node scripts/audit-nav-routes.mjs` exits 0
    - `pnpm --filter @cerebro/studio build` exits 0
  </acceptance_criteria>
  <done>Breadcrumbs and page titles both derive from the single navigation-registry lookup, and Topbar's registry-bypassing links point at destinations that exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next.js App Router | `usePathname()` output (user-controlled URL) feeds the breadcrumb lookup and `document.title` |
| registry (static module) -> rendered nav | All 99 hrefs become clickable for the first time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Information Disclosure / Tampering | `document.title` set from a path-derived value | mitigate | Set the title only from the registry entry's `item.title` (a static string authored in `navigation/index.ts`), never from the raw `pathname`. When `findNavTrailByPath` returns an empty trail, fall back to the constant `CerebroHive Studio` — do not interpolate the unmatched path into the title |
| T-01-12 | Tampering | Breadcrumb render of registry labels | mitigate | Render labels through React text interpolation only. No `dangerouslySetInnerHTML`. Labels originate from the static registry module, not from user input or an API response |
| T-01-13 | Information Disclosure | Newly-reachable nav surfaces (34 previously-hidden items) | accept | Every newly-reachable destination renders `PlaceholderModule`, which displays only a module name, a feature name and a status string. It performs no data fetch, holds no props from the network, and therefore exposes nothing that was not already in the client bundle's static registry |
| T-01-14 | Elevation of Privilege | `services/forge-api` — 9 controllers with no auth guard and no enforced tenant scoping | accept (deliberately deferred) | Known pre-existing gap, explicitly out of Phase 1 scope per CONTEXT.md D-04 and 01-VALIDATION.md's Security Domain note. This plan increases the *discoverability* of the forge routes but adds no new backend surface and no new client call. Fixing it is Phase 2 territory (WKSP-05) |
| T-01-SC | Tampering | npm/pnpm installs | mitigate | No packages installed. `lucide-react` (`ChevronRight`) and `next/link` are existing dependencies. 01-RESEARCH.md's Package Legitimacy Audit records zero new packages this phase |
</threat_model>

<verification>
1. `node scripts/audit-nav-routes.mjs` — exits 0 with all assertions green, including `SIDEBAR_HANDPICK` and `PINNED_ORPHAN` (which stayed red after plan 01-01 by design)
2. `pnpm --filter @cerebro/studio typecheck` — green
3. `pnpm --filter @cerebro/studio build` — green
4. Manual click-through of all 14 sidebar groups is owned by plan 01-07's checkpoint (01-VALIDATION.md lists it as manual-only; no sidebar-rendering test harness exists)
</verification>

<success_criteria>
- All 14 `platformNavigation` groups render from one generic loop; no group can be dropped by omission again
- The six previously-hidden groups (HiveOps, Automation, Research, Academy, Business, Support — 34 items) are reachable and visually identical to the existing ones
- Zero sidebar or Topbar href points at a destination absent from the registry
- Breadcrumbs and page titles share a single `findNavTrailByPath` call, satisfying D-08's one-source-of-truth requirement
</success_criteria>

<output>
Create `.planning/phases/01-schema-navigation-foundation/01-04-SUMMARY.md` when done.
Record in the SUMMARY: the final list of display-label overrides, every Topbar href changed and why, and whether `findNavEntryByPath` resolved group-level hrefs such as `/app/security`.
</output>
