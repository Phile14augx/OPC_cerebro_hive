# M26.1 — Architectural Baseline (Frozen)

**Status:** Frozen. This document is the stable reference point for everything built under M26.1 (`packages/engineering-review`) — implemented invariants and public extension points, as they stand pending operational verification (see `M26.1-INDEX.md` for the full document set this baseline draws from). Future slices and any M26.2/M26.3 work should treat this as the contract they build on top of, not something to casually reopen.

## What "frozen" means here

Architecturally complete, manually reviewed line by line against the domain model and ADRs, but **not yet operationally verified** — no successful `pnpm install`/`lint`/`typecheck`/`test`/`test --coverage` run has occurred (no working package registry access in the environment this was built in). "Frozen" describes the design and code as written, not a claim that it's been proven to run. Any issues that surface once verification runs should be treated as bugs against this baseline, not as license to redesign it.

## Implemented invariants (enforced in code, `packages/engineering-review/src/EngineeringReviewReport.ts`)

1. A review cannot leave `Draft` without at least one `EvidenceReference` (`collectEvidence`).
2. A `Finding` cannot be constructed without at least one `EvidenceReference` (`createReviewFinding`), and cannot be added to a review citing evidence that review never recorded (`addFinding` — throws `MissingEvidenceReference`).
3. A `Recommendation` cannot be constructed without at least one supporting `Finding` (`createReviewRecommendation`), and cannot be added citing a finding the review never recorded (`addRecommendation` — throws `MissingFindingReference`).
4. A `Published` review is immutable — every mutating method (`addEvidence`, `addFinding`, `addRecommendation`, `decideVerdict`, `publish`) throws `InvalidLifecycleTransition` once state is `Published`.
5. `EvidenceReference`s are append-only — re-adding an existing id throws `DuplicateDomainIdentifier`; there is no update/remove method at all.
6. `Finding`s never overwrite previously recorded `EvidenceReference`s — enforced by the append-only evidence list plus the cross-reference check in (2).
7. `Recommendation`s cannot introduce claims untraceable to their referenced `Finding`s — enforced structurally: a `Recommendation` has no constructor path that doesn't require `findingRefs`.
8. `PolicyEvaluation` (value object, unused as of this baseline) is recorded as a delegated outcome, not computed by the domain — the type exists so Slice 5 can populate it without a model change.
9. Every `Finding`/`Recommendation` is traceable to `Provenance` via `EvidenceReference` — structural, not a runtime check, since there's no construction path that omits it.
10. None of the above ever touches `CanPublishWorkflow` or `WorkflowValidator` — this package has zero dependency on either; nothing in it could produce that side effect even accidentally.
11. A `Verdict` cannot exist before `RecommendationsGenerated` (`decideVerdict` — throws `InvalidLifecycleTransition` if attempted earlier).
12. A `Published` review cannot exist without a `Verdict` (`publish` — throws `DomainInvariantViolation` if attempted without one).

Additional identifier-integrity guarantees added since the initial Slice 1 pass: duplicate `Finding`/`Recommendation` ids are rejected the same way duplicate `EvidenceReference` ids are (`DuplicateDomainIdentifier`), and a `Verdict` referencing an unrecorded `Recommendation` is rejected (`MissingRecommendationReference`).

## Public extension points

- **`IEngineeringReviewRepository`** (`ports/IEngineeringReviewRepository.ts`) — `save`/`load`/`findLatest`/`findByWorkflow`/`findByManifest`/`findByVerdict`. One implementation shipped: `InMemoryEngineeringReviewRepository`. A real adapter (Slice 3) implements this same interface — no change to the interface itself implied.
- **`ISnapshotProvider`** (`ports/ISnapshotProvider.ts`) — `getSnapshot(workflowId)`. One implementation shipped: `FixedSnapshotProvider`.
- **`IReviewContributor`** (`ports/IReviewContributor.ts`) — `execute(context) -> ContributorResult`. One implementation shipped: `ArchitectureReviewContributor`. This is the contract every future contributor (Security, Cost, Compliance, Release, per the Phase 6 roadmap sketch) implements.
- **`RecommendationStrategy`** (`application/RecommendationStrategy.ts`) — `synthesize(findings) -> Recommendation[]`. One implementation shipped: `OneRecommendationPerFindingStrategy`. Explicitly not assumed 1:1 by the orchestrator itself.
- **`VerdictStrategy`** (`application/VerdictStrategy.ts`) — `decide(findings, recommendations) -> Verdict`. One implementation shipped: `SeverityBasedVerdictStrategy`.
- **`EngineeringReviewOrchestrator`** (`application/EngineeringReviewOrchestrator.ts`) — the composition root all of the above plug into; takes an `ISnapshotProvider`, an `IEngineeringReviewRepository`, a contributor list, and the two strategies (both defaulted).

## Explicitly not part of this baseline

- Timeout/cancellation contributor statuses — deferred by design; a `TIMEOUT` status without real deadline/cancellation infrastructure behind it would be cosmetic (agreed explicitly in review).
- Policy-driven verdicts/recommendations — Slice 5, not built.
- Real persistence, event publication, contributor registry/DAG ordering — Slices 3 and 6, not built.
- Any API projection layer, caching strategy, or Studio UX — **not proposed as an accepted specification by this baseline.** Discussed only as name-only future direction; would need its own PRD/ADR pass before implementation, per the correction already made earlier in this project's history.

## Operational verification — attempted, blocked (2026-07-28)

`pnpm build` and `pnpm test` were run in a real environment. Findings, triaged:

- **`pnpm build`**: failed, but only in `@cerebro/ui` (71 pre-existing TS errors: missing `@storybook/react`/`vitest`/`@testing-library/react` type declarations, plus duplicate-export ambiguity in `Badge/index.ts` and `Button/index.ts`). `@cerebro/engineering-review` does not appear in this failure — unrelated to M26.1.
- **`pnpm test`**: `@cerebro/engineering-review` failed at **startup**, before any test file loaded:
  ```
  Error [ERR_PACKAGE_PATH_NOT_EXPORTED]: Package subpath './module-runner' is not defined by "exports" in .../vite/package.json
  ```
  Identical failure hit four unrelated packages (`runtime-core`, `motion`, `visualization`, `tokens`) — same error, same stack, ruling out anything specific to this package's code.

  Root cause, confirmed against the actual installed packages: root `package.json` declares an override `"vite": ">=6.0.0"`, but the lockfile has `vite@5.4.21` resolved instead. `vite@5.4.21`'s own `exports` field has no `./module-runner` subpath (added in Vite 6) — `vitest@4.1.10` (resolved from the root's `"vitest": ">=3.2.4"` override, superseding this package's own `^3.2.4` pin) requires it and fails immediately. **The lockfile is stale relative to the root's own override declaration** — a workspace-wide resolution bug, not something introduced by this package.

- **Not yet observed**: `lint`, `typecheck`, and this package's actual test assertions (lifecycle invariants, reference-integrity, orchestrator e2e) — blocked by the above before they could run.

**Status classification** (four states are more precise than a single pass/fail):

```
Architecture:            VERIFIED   (manual review against domain model + ADRs)
Implementation:          BUILT      (packages/engineering-review, Slices 1-2)
Behavioral Verification: BLOCKED    (workspace dependency resolution — see above)
```

**Important wording distinction**: behavioral verification is blocked by a workspace dependency-resolution defect. No evidence collected in this run implicates the M26.1 implementation itself, because test execution terminated before any Engineering Review test cases were discovered or executed. That supports "no defects observed" — it does not support "defects ruled out." Those are different claims, and only the first is backed by this evidence.

**Recommended remediation order** (isolates package-level verification from the unrelated workspace problem):

```bash
# 1. Resolve the Vite/Vitest version mismatch (root override says vite >=6.0.0;
#    lockfile currently resolves vite@5.4.21 — needs to actually satisfy the override)
# 2. Regenerate the lockfile
# 3. Reinstall dependencies from a clean state
# 4. Run this package in isolation first, for a clean signal:
pnpm --filter @cerebro/engineering-review lint
pnpm --filter @cerebro/engineering-review typecheck
pnpm --filter @cerebro/engineering-review test
pnpm --filter @cerebro/engineering-review test -- --coverage
# 5. Only after those pass, run the full workspace suite:
pnpm test
# 6. Then proceed to deployment verification (PLATFORM_NO_DB) and Helm rendering.
```

Until step 4 runs successfully, this baseline remains "architecturally complete, manually reviewed" — not "behaviorally verified." The one real finding so far (the vite/vitest lockfile drift) is an environment defect, not a defect in this baseline's invariants or extension points, and should not be treated as grounds to redesign either.

## Operational verification — first behavioral evidence (2026-07-28, continued)

Once run in isolation, `pnpm --filter @cerebro/engineering-review test` executed for real: **26/27 tests passed, 1 failed** — the first actual behavioral evidence for this implementation, superseding the manual-review-only status above for everything the passing tests cover.

- `EngineeringReviewReport.test.ts` (15/15), `EngineeringReviewReport.referenceIntegrity.test.ts` (6/6), `ContributorE2E.test.ts` (2/2) — all passed. The domain aggregate's lifecycle, invariants, and reference-integrity guarantees behave as designed.
- `EngineeringReviewOrchestrator.test.ts` — 3/4 passed; **1 genuine defect found**, in the orchestrator (application layer), not the domain model:
  - `addEvidence` is only valid in `Draft`; `addFinding` is only valid in `EvidenceCollected` (both enforced correctly by `EngineeringReviewReport`, per Phase 3).
  - The orchestrator was adding evidence and findings in a single pass across contributors, then calling `collectEvidence()` only afterward — so any contributor producing a finding hit `addFinding` while the review was still in `Draft`, throwing `InvalidLifecycleTransition`.
  - The "clean workflow" test happened to pass because that contributor produces zero findings, never exercising the bug — which is why manual review didn't catch it.
  - **Fixed**: `EngineeringReviewOrchestrator.run` now records all evidence and calls `collectEvidence()` before adding any findings (two passes instead of one).
  - **Confirmed (2026-07-28, re-run)**: `pnpm --filter @cerebro/engineering-review test` → **27/27 passed, 4/4 test files passed**. The fix holds.

This is exactly the kind of finding operational verification exists to catch: the domain model was correct, the orchestrator's sequencing against that model was not, and no amount of further manual review would have reliably caught it without a real test run.

## Status: test suite behaviorally verified

```
Architecture:            VERIFIED
Implementation:          BUILT
Behavioral Verification: PASSED   (27/27, packages/engineering-review test suite)
```

Not yet run: `lint`, `typecheck`, `test -- --coverage` for this package; the full workspace `pnpm test` (still blocked for other packages by the unrelated vite/vitest lockfile drift documented above); deployment verification (`PLATFORM_NO_DB`); Helm template rendering. This package's own test suite passing does not close any of those out.

## Full workspace `pnpm test` — inconclusive for this package, not a regression

A subsequent full-workspace `pnpm test` run showed `@cerebro/engineering-review:test` reporting "Test Files 0 passed (4)" / "Tests 0 passed (0)" — this is **not** a regression from the 27/27 isolated result above, and is not being treated as new evidence against this package. Reasoning:

- `@cerebro/config#build` failed in the same run for real, unrelated reasons: missing `zod` and `@types/node` devDependencies (5 TS errors — pre-existing gap in that package, no relation to `engineering-review`).
- The run's own summary reported `Tasks: 0 successful, 18 total` — every task in the scoped batch at zero successes, which is turbo's bail-on-first-failure behavior: once `@cerebro/config#build` failed, turbo killed the other concurrently-running tasks, including this package's test process, mid-execution.
- The "0 passed (4)" line lists no actual assertion or error output, unlike the real orchestrator failure caught earlier (which showed a specific stack trace). Consistent with an interrupted process, not a genuine 0/4 result.

The isolated run (`pnpm --filter @cerebro/engineering-review test` → 27/27) remains the valid signal for this package. Re-confirming via the full workspace suite requires either fixing `@cerebro/config`'s missing dependencies first, or running with turbo's bail behavior disabled (`--continue`), neither of which has been done yet.
