---
phase: 01-schema-navigation-foundation
plan: 06
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/studio/app/(platform)/app/hiveops/page.tsx
  - apps/studio/app/(platform)/app/hiveops/pipelines/page.tsx
  - apps/studio/app/(platform)/app/hiveops/deployments/page.tsx
  - apps/studio/app/(platform)/app/hiveops/clusters/page.tsx
  - apps/studio/app/(platform)/app/hiveops/security/page.tsx
  - apps/studio/app/(platform)/app/hiveops/costs/page.tsx
  - apps/studio/app/(platform)/app/hiveops/gitops/page.tsx
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "A user visiting any HiveOps destination sees an honest 'not yet available' page, not invented pipeline runs, cluster health or AI cost figures"
    - "The HiveOps pages are driven by the same shared placeholder component as every other unimplemented module"
  artifacts:
    - path: "apps/studio/app/(platform)/app/hiveops/costs/page.tsx"
      provides: "AI Costs placeholder replacing fabricated spend figures"
      contains: "PlaceholderModule"
    - path: "apps/studio/app/(platform)/app/hiveops/gitops/page.tsx"
      provides: "GitOps placeholder replacing 304 lines of invented sync state"
      contains: "PlaceholderModule"
  key_links:
    - from: "apps/studio/app/(platform)/app/hiveops/page.tsx"
      to: "apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx"
      via: "component render"
      pattern: "PlaceholderModule"
---

<objective>
Replace HiveOps' seven fully-built mock pages with the shared honest placeholder, closing the gap between "the route resolves" and "the route tells the truth".

Purpose: CONTEXT.md D-05 names HiveOps (7 items) in the list of groups that "all get a standardized, registry-driven honest placeholder this phase. No functionality build for any of them." ROADMAP success criterion 3 repeats it. Unlike the other seven groups in D-05, HiveOps' pages already exist — and they render invented pipeline runs, deployment history, cluster health, security findings, GitOps sync state and AI spend figures with zero backend calls. Leaving them in place would satisfy NAV-01's "no 404" while directly violating the project's core value.
Output: 7 pages, each a single `PlaceholderModule` render with the exact nav-registry title.
</objective>

<phase_goal>
**As a** CerebroHive Studio user, **I want to** click any destination in the sidebar and land on either a working page or a clearly-labelled "not yet available" page, **so that** I never hit a 404, a dead link, or fabricated data while navigating the product.

This plan owns the "never fabricated data" half for the HiveOps group, which plan 01-04 makes reachable in the sidebar for the first time.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-navigation-foundation/01-CONTEXT.md
@.planning/phases/01-schema-navigation-foundation/01-RESEARCH.md
@.planning/phases/01-schema-navigation-foundation/01-UI-SPEC.md
@.planning/phases/01-schema-navigation-foundation/01-01-SUMMARY.md
@.planning/phases/01-schema-navigation-foundation/01-05-SUMMARY.md

<scope_note>
01-RESEARCH.md and 01-PATTERNS.md scoped fake-data removal to CerebroForge's 10 unbacked pages only, and describe HiveOps merely as "has full mock-UI pages for all 7 items" without listing it as removal work. That is a coverage gap in those documents relative to CONTEXT.md D-05 and ROADMAP success criterion 3, both of which put HiveOps in the placeholder set. This plan closes that gap. It applies the identical treatment plan 01-05 applies to CerebroForge.
</scope_note>

<interfaces>
Consumed from plan 01-01:

  // apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx
  export function PlaceholderModule(props: {
    group: string;
    title: string;
    status: "planned" | "disabled";
  }): JSX.Element

Import path from `hiveops/page.tsx`: `../components/ui/PlaceholderModule`.
Import path from `hiveops/<sub>/page.tsx`: `../../components/ui/PlaceholderModule`.

**Exact `title` values — copy verbatim from `hiveopsNavigation.items` in `apps/studio/app/(platform)/app/navigation/index.ts` lines 232-245.** The eyebrow must match the sidebar label the user clicked:

| Route | `title` prop (exact) | current lines |
|---|---|---|
| `/app/hiveops`             | `Overview`    | 199 |
| `/app/hiveops/pipelines`   | `Pipelines`   | 279 |
| `/app/hiveops/deployments` | `Deployments` | 206 |
| `/app/hiveops/security`    | `Security`    | 262 |
| `/app/hiveops/costs`       | `AI Costs`    | 211 |
| `/app/hiveops/clusters`    | `Clusters`    | 211 |
| `/app/hiveops/gitops`      | `GitOps`      | 304 |

`group` is `HiveOps` for all seven. `status` is `planned` for all seven, matching the `implementationStatus` plan 01-01 assigned to these same items.
</interfaces>

<scope_guard>
- Replace the entire page body with a single `PlaceholderModule` render. Keep no decorative chrome, no per-page icons, no status badges, no time-range selectors — all of it implies functionality that does not exist.
- Do NOT delete the `page.tsx` files. The literal static route must keep winning over the catch-all so the route stays stable for any future phase that implements HiveOps (D-09).
- Do NOT build any HiveOps backend, API route, or data source. D-05 is explicit: no functionality build for any of these groups this phase.
- Do NOT touch `apps/studio/app/(platform)/app/forge/**` — plan 01-05 owns those files in the same wave.
</scope_guard>

<decisions_implemented>
This plan is the direct implementation of:
- **D-05:** HiveOps (7 items) gets the standardized, registry-driven honest placeholder — no functionality build — matching the treatment specified for all 8 previously-unscoped groups.
</decisions_implemented>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the mock UI on HiveOps Overview, Pipelines, Deployments and Clusters</name>
  <files>apps/studio/app/(platform)/app/hiveops/page.tsx, apps/studio/app/(platform)/app/hiveops/pipelines/page.tsx, apps/studio/app/(platform)/app/hiveops/deployments/page.tsx, apps/studio/app/(platform)/app/hiveops/clusters/page.tsx</files>
  <read_first>
    - apps/studio/app/(platform)/app/hiveops/page.tsx — 199 lines, 3 hardcoded data arrays
    - apps/studio/app/(platform)/app/hiveops/pipelines/page.tsx — 279 lines, invented pipeline runs
    - apps/studio/app/(platform)/app/hiveops/deployments/page.tsx — 206 lines, invented deployment history
    - apps/studio/app/(platform)/app/hiveops/clusters/page.tsx — 211 lines, invented cluster health
    - apps/studio/app/(platform)/app/components/ui/PlaceholderModule.tsx — the component created by plan 01-01, for its exact prop shape
    - apps/studio/app/(platform)/app/navigation/index.ts — lines 232-245, the authoritative `hiveopsNavigation` item titles
    - apps/studio/app/(platform)/app/forge/backend/page.tsx — the finished conversion from plan 01-05, as the in-repo shape reference
  </read_first>
  <action>
Before replacing each page, confirm the claim that it renders only fabricated data: search the file for `fetch(`, `useSWR`, `useQuery`, any `use*` data hook, and any `/api/` path. All four of these pages were measured at plan time to have zero such calls. If any page turns out to make a genuine backend call returning real data, **stop and report it in the SUMMARY instead of converting it** — demoting a real page to a placeholder would be a regression, and the classification recorded in `01-NAV-STATUS.md` would need correcting first.

For each confirmed-mock page, replace the whole file with a `"use client"` module whose default export is a single-expression component returning `<PlaceholderModule group="HiveOps" title="<exact title>" status="planned" />`. Titles: `Overview`, `Pipelines`, `Deployments`, `Clusters`.

Keep each file's existing default-export component name so any external reference stays valid, and drop every other symbol: the hardcoded data arrays, all chart and stat rendering, every `useState` and effect, and every now-unused `lucide-react` and design-system import. A leftover unused import is a lint failure and a signal the removal was incomplete.
  </action>
  <verify>
    <automated>pnpm --filter @cerebro/studio typecheck</automated>
    <automated>for p in "apps/studio/app/(platform)/app/hiveops/page.tsx" "apps/studio/app/(platform)/app/hiveops/pipelines/page.tsx" "apps/studio/app/(platform)/app/hiveops/deployments/page.tsx" "apps/studio/app/(platform)/app/hiveops/clusters/page.tsx"; do s=$(grep -v '^[[:space:]]*//' "$p"); pm=$(printf '%s' "$s" | grep -c 'PlaceholderModule'); sc=$(printf '%s' "$s" | grep -c 'StatCard'); us=$(printf '%s' "$s" | grep -c 'useState'); n=$(wc -l < "$p"); echo "$p placeholder=$pm statcard=$sc usestate=$us lines=$n"; test "$pm" -ge 1 -a "$sc" -eq 0 -a "$us" -eq 0 -a "$n" -lt 15 || exit 1; done; echo GATE-OK</automated>
    <automated>pnpm --filter @cerebro/studio lint</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four files contains `PlaceholderModule` and, with `//` comment lines stripped, zero occurrences of `StatCard`, `useState`, `setTimeout`, `Suspense` and `useSearchParams`
    - Each file's `title` prop matches its `hiveopsNavigation` entry character-for-character (`Overview`, `Pipelines`, `Deployments`, `Clusters`)
    - Each file passes `group="HiveOps"` and `status="planned"`
    - Each file is under 15 lines and still exists at its original path
    - Each file contains zero occurrences of `fetch(` and zero occurrences of `/api/`
    - `pnpm --filter @cerebro/studio lint` reports no unused-import errors in these four files
    - `pnpm --filter @cerebro/studio typecheck` exits 0
  </acceptance_criteria>
  <done>HiveOps Overview, Pipelines, Deployments and Clusters render the shared honest placeholder with zero invented operational data.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the mock UI on HiveOps Security, AI Costs and GitOps</name>
  <files>apps/studio/app/(platform)/app/hiveops/security/page.tsx, apps/studio/app/(platform)/app/hiveops/costs/page.tsx, apps/studio/app/(platform)/app/hiveops/gitops/page.tsx</files>
  <read_first>
    - apps/studio/app/(platform)/app/hiveops/security/page.tsx — 262 lines; a plan-time scan matched one occurrence of a data-call pattern in this file, so it must be inspected carefully before conversion (see the action's verification step)
    - apps/studio/app/(platform)/app/hiveops/costs/page.tsx — 211 lines, 5 hardcoded data arrays, invented AI spend figures
    - apps/studio/app/(platform)/app/hiveops/gitops/page.tsx — 304 lines, invented sync state; the largest page in the group
    - apps/studio/app/(platform)/app/navigation/index.ts — lines 232-245, the authoritative titles
    - apps/studio/app/(platform)/app/hiveops/page.tsx — the finished conversion from Task 1, as the shape reference
  </read_first>
  <action>
**`hiveops/security/page.tsx` requires a decision before conversion.** A plan-time pattern scan matched one data-call-shaped construct in this file, unlike its six siblings which matched none. Read the file and determine whether that match is a genuine backend call returning real data or a false positive (for example the literal string `/api/` inside hardcoded mock content, or an unrelated identifier). If it is a genuine, working backend call, do NOT convert the page — record the finding in the SUMMARY, leave the page as-is, and flag that `01-NAV-STATUS.md` should reclassify `/app/hiveops/security` from `planned` to `active`. If it is a false positive or a dead call, convert it like the others.

For each page confirmed as mock-only, replace the whole file with a `"use client"` module whose default export is a single-expression component returning `<PlaceholderModule group="HiveOps" title="<exact title>" status="planned" />`. Titles: `Security`, `AI Costs`, `GitOps`.

`hiveops/costs/page.tsx` is the highest-risk page in this group for user harm: it presents invented AI spend figures that a user could reasonably act on financially. Every fabricated cost array and every derived total must go.

Drop every non-default-export symbol, all hardcoded data arrays, all chart and stat rendering, every `useState` and effect, and every now-unused import.

After all seven pages are handled, run a repo-wide check that no page under `apps/studio/app/(platform)/app/hiveops/` still imports `StatCard` or declares a top-level array literal of mock records, and record the result in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm --filter @cerebro/studio typecheck</automated>
    <automated>for p in "apps/studio/app/(platform)/app/hiveops/costs/page.tsx" "apps/studio/app/(platform)/app/hiveops/gitops/page.tsx"; do s=$(grep -v '^[[:space:]]*//' "$p"); pm=$(printf '%s' "$s" | grep -c 'PlaceholderModule'); sc=$(printf '%s' "$s" | grep -c 'StatCard'); n=$(wc -l < "$p"); echo "$p placeholder=$pm statcard=$sc lines=$n"; test "$pm" -ge 1 -a "$sc" -eq 0 -a "$n" -lt 15 || exit 1; done; echo GATE-OK</automated>
    <automated>grep -rn "StatCard" "apps/studio/app/(platform)/app/hiveops/" || echo NO-STATCARD-IN-HIVEOPS</automated>
    <automated>pnpm --filter @cerebro/studio lint</automated>
    <automated>pnpm --filter @cerebro/studio build</automated>
  </verify>
  <acceptance_criteria>
    - `hiveops/costs/page.tsx` and `hiveops/gitops/page.tsx` each contain `PlaceholderModule`, are under 15 lines, and contain zero occurrences of `StatCard`, `useState` and `setTimeout` after stripping `//` comment lines
    - `hiveops/costs/page.tsx` passes the literal title `AI Costs`; `hiveops/gitops/page.tsx` passes `GitOps`
    - `hiveops/security/page.tsx` is either converted to `PlaceholderModule` with title `Security`, OR left unchanged with a written justification in the SUMMARY naming the genuine backend call it makes and the required `01-NAV-STATUS.md` reclassification
    - The repo-wide check reports either `NO-STATCARD-IN-HIVEOPS` or, if `hiveops/security/page.tsx` was legitimately kept, only that file
    - All seven HiveOps route files still exist at their original paths
    - `pnpm --filter @cerebro/studio lint` and `pnpm --filter @cerebro/studio build` both exit 0
  </acceptance_criteria>
  <done>Every HiveOps destination either renders the shared honest placeholder or is documented as genuinely backend-backed; no invented pipeline, cluster, security or spend data remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user perception -> displayed data | Invented operational and financial figures presented as live telemetry is an integrity failure a user can act on |
| browser -> backend | These 7 routes make no backend call before or after this plan (pending Task 2's verification of `hiveops/security`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Tampering (data integrity, user-facing) | `hiveops/costs/page.tsx` invented AI spend figures | mitigate | Delete every fabricated cost array and derived total; replace the page body with `PlaceholderModule`. Verified by a grep gate for `StatCard` and a size gate under 15 lines. Highest severity in the group because fabricated spend data could drive a real budget decision |
| T-01-19 | Tampering (data integrity, user-facing) | `hiveops/security/page.tsx` invented security findings | mitigate | Same treatment, gated on Task 2's verification that the file makes no genuine backend call. Fabricated security posture could cause a user to believe a control is in place when it is not |
| T-01-20 | Repudiation | Invented pipeline, deployment and GitOps sync history | mitigate | Delete every hardcoded record array; the placeholder makes the absence of history explicit rather than presenting an invented one |
| T-01-21 | Elevation of Privilege | `services/forge-api` — no auth guard, no enforced tenant scoping | accept (deliberately deferred) | Known pre-existing gap, explicitly out of Phase 1 scope per CONTEXT.md D-04 and 01-VALIDATION.md's Security Domain note. Unrelated to HiveOps' files; recorded for phase-wide continuity. This plan strictly reduces exposure — seven pages that rendered interactive chrome now render static informational text and make zero network calls |
| T-01-SC | Tampering | npm/pnpm installs | mitigate | No packages installed; this plan only removes code and imports. 01-RESEARCH.md's Package Legitimacy Audit records zero new packages this phase |
</threat_model>

<verification>
1. Grep and size gates in both tasks — `GATE-OK`
2. Repo-wide HiveOps `StatCard` check — `NO-STATCARD-IN-HIVEOPS` (or a documented exception for `hiveops/security`)
3. `pnpm --filter @cerebro/studio typecheck` — green
4. `pnpm --filter @cerebro/studio lint` — green, no unused-import errors
5. `pnpm --filter @cerebro/studio build` — green
6. `node scripts/audit-nav-routes.mjs` — these seven routes keep their literal `page.tsx`, so `UNRESOLVED_ROUTE` stays green
</verification>

<success_criteria>
- All 7 HiveOps destinations render `PlaceholderModule` with their exact registry title, or are documented as genuinely backend-backed
- Zero invented pipeline runs, deployment history, cluster health readings, security findings, GitOps sync state or AI spend figures remain
- Every route file survives, keeping the route stable for a future HiveOps phase (D-09)
- No HiveOps backend or API route was created (D-05)
</success_criteria>

<output>
Create `.planning/phases/01-schema-navigation-foundation/01-06-SUMMARY.md` when done.
Record in the SUMMARY: the before/after line count per page, the verdict on `hiveops/security/page.tsx`'s data-call match, and any required `01-NAV-STATUS.md` correction.
</output>
