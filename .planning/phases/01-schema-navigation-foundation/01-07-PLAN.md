---
phase: 01-schema-navigation-foundation
plan: 07
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04", "01-05", "01-06"]
files_modified:
  - .planning/phases/01-schema-navigation-foundation/01-VALIDATION.md
  - .planning/phases/01-schema-navigation-foundation/01-NAV-STATUS.md
autonomous: false
requirements: [NAV-01, NAV-02, FORGE-01, FORGE-02, SCHM-01, SCHM-02]
must_haves:
  truths:
    - "A live click-through of all 14 sidebar groups produces zero 404s and zero fabricated numbers"
    - "All 9 backed CerebroForge pages return a real forge-api result against a running service, or a visible error with a working retry"
    - "The whole monorepo passes lint, typecheck, test and build after the phase's changes"
  artifacts:
    - path: ".planning/phases/01-schema-navigation-foundation/01-VALIDATION.md"
      provides: "Per-task verification map populated with real task IDs and pass/fail status"
      contains: "nyquist_compliant"
  key_links:
    - from: "scripts/audit-nav-routes.mjs"
      to: "apps/studio/app/(platform)/app/navigation/index.ts"
      via: "static registry-vs-route audit"
      pattern: "audit-nav-routes"
---

<objective>
Prove the phase actually delivered, using the two checks no automated harness in this repo can perform: a live click-through of every sidebar group, and a live run of the 9 backed CerebroForge pages against a running `forge-api`. Run the full-program build gate first so the human check is not spent on a broken tree.

Purpose: 01-VALIDATION.md lists both of these as manual-only — no sidebar-rendering test and no cross-service integration harness exists in this repo today. This plan is the phase gate that runs before `/gsd:verify-work`.
Output: a green full-program build, an updated `01-VALIDATION.md` with real task IDs and statuses, and a recorded human sign-off.
</objective>

<phase_goal>
**As a** CerebroHive Studio user, **I want to** click any destination in the sidebar and land on either a working page or a clearly-labelled "not yet available" page, **so that** I never hit a 404, a dead link, or fabricated data while navigating the product.

This plan verifies the story end to end, by a real person actually clicking.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-navigation-foundation/01-CONTEXT.md
@.planning/phases/01-schema-navigation-foundation/01-VALIDATION.md
@.planning/phases/01-schema-navigation-foundation/01-01-SUMMARY.md
@.planning/phases/01-schema-navigation-foundation/01-02-SUMMARY.md
@.planning/phases/01-schema-navigation-foundation/01-03-SUMMARY.md
@.planning/phases/01-schema-navigation-foundation/01-04-SUMMARY.md
@.planning/phases/01-schema-navigation-foundation/01-05-SUMMARY.md
@.planning/phases/01-schema-navigation-foundation/01-06-SUMMARY.md

<phase_success_criteria>
From ROADMAP.md Phase 1, the five statements this gate must confirm:
1. All 99 sidebar nav destinations render a real page or an honest "not yet available" placeholder — never a 404
2. The 9 CerebroForge items with an existing forge-api controller are wired to real, working functionality
3. Every non-functional destination renders from a single registry-driven placeholder component keyed by `implementationStatus` — not one-off hardcoded pages
4. Governance, Talent OS and Explore's Prisma schema gaps are confirmed model-by-model
5. `archive-worker` and `archive-api` run on a single reconciled BullMQ major version
</phase_success_criteria>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full-program gate and populate the validation record</name>
  <files>.planning/phases/01-schema-navigation-foundation/01-VALIDATION.md, .planning/phases/01-schema-navigation-foundation/01-NAV-STATUS.md</files>
  <read_first>
    - .planning/phases/01-schema-navigation-foundation/01-VALIDATION.md — the Per-Task Verification Map whose placeholder `01-01-XX` task IDs must be replaced with the real ones, and the frontmatter fields `nyquist_compliant` and `wave_0_complete`
    - .planning/phases/01-schema-navigation-foundation/01-NAV-STATUS.md — the per-item classification table written by plan 01-01; plans 01-05 and 01-06 may have required corrections
    - .planning/phases/01-schema-navigation-foundation/01-06-SUMMARY.md — specifically the verdict on `hiveops/security/page.tsx`, which may require a `01-NAV-STATUS.md` reclassification
    - scripts/audit-nav-routes.mjs — the assertions being run
  </read_first>
  <action>
Run the full-program gate in this order, recording each command's exit code and output tail:

  1. `node scripts/audit-nav-routes.mjs` — every assertion must be green, including `SIDEBAR_HANDPICK` and `PINNED_ORPHAN` which plan 01-04 was responsible for closing
  2. `pnpm vitest run --config vitest.studio.config.ts --reporter=dot` — the `PlaceholderModule` render test
  3. `pnpm --filter @cerebro/db exec prisma migrate status` — must report no pending migrations (this is the SCHM-01 blocking gate from plan 01-03; a green build with a pending migration is a false positive)
  4. `pnpm typecheck` — whole monorepo
  5. `pnpm lint` — whole monorepo
  6. `pnpm test` — whole monorepo
  7. `pnpm build` — whole monorepo

Then run a phase-wide fabricated-content sweep across the 17 pages converted by plans 01-05 and 01-06 plus the catch-all: assert that, with `//` comment lines stripped, no file under `apps/studio/app/(platform)/app/forge/{backend,database,api,mobile,web,desktop,bots,repos,ui-studio,monitoring}/` or `apps/studio/app/(platform)/app/hiveops/` contains `StatCard` or `setTimeout`, and that every one of those files is under 15 lines.

Update `.planning/phases/01-schema-navigation-foundation/01-VALIDATION.md`:
  - Replace every `01-01-XX` placeholder task ID in the Per-Task Verification Map with the real plan and task identifiers now that they exist: NAV-01's route audit is plan 01-01 task 1 and 2 plus plan 01-04 task 1; NAV-02's placeholder render test is plan 01-01 task 1 and 2; FORGE-01 is plan 01-02 tasks 1-3; SCHM-01 is plan 01-03 tasks 1-2; SCHM-02 is plan 01-03 task 3. Add rows for FORGE-02 (plan 01-05) and for the HiveOps placeholder conversion (plan 01-06), neither of which the original table covered.
  - Correct SCHM-02's automated command: `pnpm --filter @cerebro/archive-worker typecheck` is invalid — that package has no `typecheck` script and no `src/` directory. Replace it with the BullMQ major-version comparison used in plan 01-03 task 3.
  - Set each row's Status to the observed result.
  - Set `wave_0_complete: true` and `nyquist_compliant: true` in the frontmatter only if every row has a real automated command or a documented manual-only justification.
  - Tick the Validation Sign-Off checkboxes that the observed results support; leave unsupported ones unticked with a one-line reason.

Reconcile `01-NAV-STATUS.md` with the actual final state: if plan 01-06 found `hiveops/security/page.tsx` to be genuinely backend-backed, change its row from `planned` to `active` and note the correction. Do not silently rewrite history — append a "Corrections" section listing each change and its source SUMMARY.

If any gate fails, stop and report the failure rather than proceeding to the human checkpoint. Do not present a broken tree for manual verification.
  </action>
  <verify>
    <automated>node scripts/audit-nav-routes.mjs</automated>
    <automated>pnpm vitest run --config vitest.studio.config.ts --reporter=dot</automated>
    <automated>pnpm --filter @cerebro/db exec prisma migrate status</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-nav-routes.mjs` exits 0 with every assertion green, including `SIDEBAR_HANDPICK` and `PINNED_ORPHAN`
    - `pnpm --filter @cerebro/db exec prisma migrate status` reports no pending migrations
    - `pnpm typecheck`, `pnpm lint`, `pnpm test` and `pnpm build` all exit 0
    - The fabricated-content sweep finds zero `StatCard` and zero `setTimeout` occurrences across the 17 converted pages, and all 17 are under 15 lines
    - `01-VALIDATION.md` contains no remaining `01-01-XX` placeholder task ID
    - `01-VALIDATION.md` contains no reference to `pnpm --filter @cerebro/archive-worker typecheck`
    - `01-VALIDATION.md` contains rows covering all six phase requirement IDs: SCHM-01, SCHM-02, NAV-01, NAV-02, FORGE-01, FORGE-02
    - `01-NAV-STATUS.md` has a Corrections section, or an explicit statement that no correction was required
  </acceptance_criteria>
  <done>The whole program is green and the validation record reflects the real, observed state of every requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human click-through of all 14 sidebar groups and the 9 live CerebroForge tools</name>
  <what-built>
Phase 1 changed Studio's navigation and CerebroForge surfaces:
- The navigation registry gained an `implementationStatus` field on all 99 items, and a catch-all route now renders a shared `PlaceholderModule` for every destination without a real page
- The sidebar renders all 14 groups instead of 6; HiveOps, Automation, Research, Academy, Business and Support (34 items) are reachable for the first time
- A breadcrumb strip and page title now derive from the same registry entry
- The 9 backed CerebroForge tools render errors with a working `Try again` and state `No project selected` when no project is set
- 10 unbacked CerebroForge pages and 7 HiveOps pages had their fabricated statistics, invented record lists and `setTimeout` fake actions deleted and replaced with the shared placeholder
- `Policy` is now organization-scoped and migrated; `archive-worker` is on BullMQ v6

All automated gates in Task 1 are green. The two checks below have no automated harness in this repo (01-VALIDATION.md, Manual-Only Verifications), so they need your eyes.
  </what-built>
  <how-to-verify>
**Setup**

1. Start both services: `pnpm --filter @cerebro/forge-api dev` (expects port 4005) and `pnpm --filter @cerebro/studio dev` (port 3401).
2. Confirm forge-api is alive: open `http://localhost:4005/health` and `http://localhost:4005/forge/projects`. If either fails, forge-api is not running and Check B cannot be judged.
3. Open `http://localhost:3401/app`.

**Check A — sidebar reachability and honesty (NAV-01, NAV-02, D-13)**

4. Count the group headers in the sidebar. You should see **14** groups. Confirm all six previously-missing ones are present: HiveOps, Automation, Research, Academy, Business, Support.
5. Expand every group and click every item. For each one, confirm you get either a real page or the standardized placeholder card reading `Not yet available` with an eyebrow in the form `{Group} / {Item}` and a `Status: Planned` badge. **A Next.js 404 anywhere is a failure.**
6. Click all three Pinned entries at the top of the sidebar. Confirm `Workflow Builder` opens `/app/automation/builder` and does not 404.
7. Visit `/app/hiveops`, `/app/hiveops/costs` and `/app/hiveops/gitops`. Confirm each shows the placeholder — **no invented pipeline runs, no cluster health readings, no AI spend figures.**
8. Visit `/app/forge/backend` and `/app/forge/monitoring`. Confirm each shows the placeholder — **no fabricated stat cards, no "Generate Backend" button.**
9. Confirm the breadcrumb strip in the header shows the correct `{Group} / {Item}` for the page you are on, and that the browser tab title matches the item name.
10. Type a deliberately bogus URL such as `http://localhost:3401/app/does-not-exist`. Confirm you get the placeholder with an `Unknown / ...` eyebrow, **not** a 404 page.

**Check B — the 9 live CerebroForge tools (FORGE-01, D-04)**

11. Open `/app/forge`. Confirm the project list loads real data from forge-api (or shows an honest empty state if there are no projects — not a stuck skeleton).
12. Create or select a project so you have a `projectId`, then visit each of: AI Planner, Requirements Studio, Architecture Studio, Code Generation, Testing Intelligence, AI Code Review, Deployment Studio, AI Documentation. On each, run the tool's primary action and confirm a **real result renders** — not a spinner that never resolves, not a silently swallowed failure.
13. Now stop forge-api (Ctrl+C) and re-run one tool's action. Confirm you see the red error banner with the warning icon **and** a `Try again` button, and that clicking `Try again` re-attempts that same operation. Restart forge-api and confirm `Try again` then succeeds.
14. Visit any of the 8 project-scoped tools **without** a project selected. Confirm it states `No project selected` rather than sitting blank or silent.

**Report back**

For each numbered step, report pass or fail. For any failure, include the URL, what you expected, and what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" if every step passed, or describe the failing steps with their URLs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Studio dev server (port 3401) | Manual verification exercises every route including user-typed bogus paths |
| browser -> forge-api (port 4005) | Manual verification exercises all 9 backed controllers, including their failure path |
| local shell -> Postgres | `prisma migrate status` reads live migration state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22 | Repudiation (false-positive verification) | The phase gate itself | mitigate | `prisma migrate status` runs as an explicit gate because typecheck and build both pass against a stale database — the generated Prisma client derives from `schema.prisma`, not from the live schema. Without this check the phase could report green while Phase 5's first real query would fail |
| T-01-23 | Information Disclosure | Step 13's deliberate forge-api outage | mitigate | Confirm the rendered error banner shows only the composed `forge-api {status}: {body}` message. Report as a failure if it exposes a stack trace, an internal file path, the resolved `NEXT_PUBLIC_FORGE_API_URL`, or request headers |
| T-01-24 | Elevation of Privilege | `services/forge-api` — 9 controllers with no `@UseGuards` and no enforced tenant/org scoping; `projects.list()` accepts an unenforced `workspaceId`/`organizationId` filter | accept (deliberately deferred) | **Known pre-existing gap, explicitly deferred, not closed by this phase.** Recorded per 01-VALIDATION.md's Security Domain note so the verifier does not read "9 pages wired and manually confirmed working" as "9 pages secured". CONTEXT.md D-04 keeps backend hardening out of Phase 1; the shared tenant-scoping helper is WKSP-05 in Phase 2. The Phase 2 planner must inherit this as an open item |
| T-01-25 | Tampering | Catch-all route shadowing future real routes | accept | Verified structurally in plan 01-01 and behaviourally at step 10 here: static routes win, the catch-all only receives unmatched paths |
| T-01-SC | Tampering | npm/pnpm installs | mitigate | No packages installed by this plan. 01-RESEARCH.md's Package Legitimacy Audit records zero new packages across the whole phase, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` verdicts, so no blocking legitimacy checkpoint applies |
</threat_model>

<verification>
1. All Task 1 automated gates green, including the `prisma migrate status` blocking gate
2. Task 2 human sign-off recorded, with every failing step listed and its URL
3. `01-VALIDATION.md` frontmatter reflects the observed state (`wave_0_complete`, `nyquist_compliant`)
</verification>

<success_criteria>
- Whole-program lint, typecheck, test and build are green
- The nav route audit passes every assertion
- A human confirms 14 sidebar groups, zero 404s across all 99 destinations, and zero fabricated numbers on the 17 converted pages
- A human confirms the 9 backed CerebroForge tools return real forge-api results, and that the failure path shows an error banner with a working `Try again`
- The deferred forge-api auth and tenant-scoping gap is recorded as an open item for Phase 2, not silently closed
</success_criteria>

<output>
Create `.planning/phases/01-schema-navigation-foundation/01-07-SUMMARY.md` when done.
Record in the SUMMARY: every command's exit code, the human's step-by-step pass/fail report, any destination that still 404s or still shows fabricated data, and the explicit carry-forward of the forge-api auth gap to Phase 2.
</output>
